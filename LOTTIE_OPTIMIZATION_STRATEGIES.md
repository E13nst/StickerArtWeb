# 5 стратегий оптимизации Lottie-анимаций

## Стратегия A: Оптимизация загрузки (Lazy + Prefetch + Batching)

### Что делает
**Приоритет**: Фаза 1 и Фаза 2

- **Lazy loading через IntersectionObserver**: Загрузка JSON только для анимаций в viewport
- **Prefetch следующего стикера**: Предзагрузка JSON для следующего стикера в ротации `PackCard`
- **Batching загрузок**: Координация параллельных fetch (лимит 4-6 одновременных)
- **Очередь с приоритетами**: Видимые анимации → prefetch → фоновые
- **Дедупликация**: Глобальная карта загрузок для одного `fileId`

### Архитектура
```typescript
// Глобальная очередь загрузки
class AnimationLoader {
  private loading = new Map<string, Promise<any>>();
  private queue: Array<{fileId: string, priority: number, resolve: Function}> = [];
  private inFlight = 0;
  private maxConcurrent = 5;
  
  async load(fileId: string, url: string, priority: number): Promise<any> {
    // Дедупликация
    if (this.loading.has(fileId)) {
      return this.loading.get(fileId);
    }
    
    // Добавляем в очередь с приоритетом
    return new Promise((resolve) => {
      this.queue.push({fileId, url, priority, resolve});
      this.processQueue();
    });
  }
  
  private processQueue() {
    // Сортируем по приоритету и загружаем лимит
    const sorted = this.queue.sort((a, b) => b.priority - a.priority);
    while (this.inFlight < this.maxConcurrent && sorted.length > 0) {
      const item = sorted.shift();
      this.loadItem(item);
    }
  }
}
```

### Прирост производительности

| Метрика | До | После | Прирост |
|---------|-----|-------|---------|
| **TTFI** | 2-4s | 1-2s | **50%** |
| **FPS** | 30-45 | 40-50 | **25%** |
| **Сетевой трафик** | 5-10MB | 1-3MB | **70%** |
| **Память** | 200-300MB | 120-180MB | **40%** |

### Риски
- ✅ **Низкий**: Использует стандартные Web API (IntersectionObserver, fetch)
- ⚠️ **Средний**: Сложность реализации очереди с приоритетами (нужно тестирование)
- ⚠️ **Низкий**: IntersectionObserver не поддерживается в IE11 (у нас mobile-first)

### Можно внедрить БЕЗ изменений backend
- ✅ **Lazy loading** через IntersectionObserver (100% frontend)
- ✅ **Prefetch JSON** в `useStickerRotation.ts` (как сейчас для изображений)
- ✅ **Batching** через глобальную очередь загрузок
- ✅ **Дедупликация** по `fileId` в кеше
- ✅ **Приоритеты** на основе `isVisible` флага

### Сложность внедрения
- **Время**: 4-6 часов
- **Файлы**: `AnimatedSticker.tsx`, `useStickerRotation.ts`, новый `AnimationLoader.ts`
- **Тестирование**: Проверка ленивой загрузки на медленном 3G

---

## Стратегия B: Оптимизация декодирования/рендеринга (Web Workers + Offscreen Canvas)

### Что делает
**Приоритет**: Фаза 3

- **Web Workers для парсинга JSON**: Перенос парсинга больших JSON (>50KB) в отдельный поток
- **Offscreen Canvas для рендеринга**: Рендеринг на offscreen canvas + перенос кадров в основной поток
- **Адаптивное качество**: Снижение FPS на слабых устройствах (30fps вместо 60fps)
- **Canvas renderer для сложных анимаций**: Переключение с SVG на Canvas для анимаций >100 слоев
- **Батчинг кадров**: Рендер кадров блоками через `requestAnimationFrame`

### Архитектура
```typescript
// Web Worker для парсинга JSON
// animation-parser.worker.ts
self.onmessage = (e) => {
  const {jsonString} = e.data;
  const data = JSON.parse(jsonString); // Парсинг в воркере
  self.postMessage({data});
};

// AnimatedSticker.tsx
const parseInWorker = async (jsonString: string) => {
  const worker = new Worker('animation-parser.worker.ts');
  const promise = new Promise((resolve) => {
    worker.onmessage = (e) => resolve(e.data.data);
  });
  worker.postMessage({jsonString});
  return promise;
};

// Offscreen Canvas для тяжелых анимаций
const offscreenCanvas = new OffscreenCanvas(width, height);
const ctx = offscreenCanvas.getContext('2d');
// Рендерим анимацию на offscreen
const frame = offscreenCanvas.transferToImageBitmap();
// Переносим кадр в основной поток
```

### Прирост производительности

| Метрика | До | После | Прирост |
|---------|-----|-------|---------|
| **FPS** | 30-45 | 50-60 | **40%** |
| **Micro-freezes** | 50-200ms | 0-20ms | **90%** |
| **Главный поток idle** | 20% | 60% | **3x** |
| **Battery drain** | Высокий | Средний | **40%** |

### Риски
- ⚠️ **Средний**: Web Workers добавляют сложность (передача данных, сериализация)
- ⚠️ **Средний**: Offscreen Canvas не поддерживается в Safari < 16.4 (mobile)
- 🔴 **Высокий**: Offscreen Canvas в Worker требует Chrome только (нет fallback)
- ⚠️ **Низкий**: Увеличение bundle size на worker (~20KB)

### Можно внедрить БЕЗ изменений backend
- ✅ **Web Worker парсинг** (100% frontend, но нужен worker файл)
- ❌ **Offscreen Canvas в Worker** (ограниченная поддержка, риск для mobile)
- ✅ **Canvas renderer** для сложных анимаций (опция `lottie-react`)
- ✅ **Адаптивное качество** (настройки `rendererSettings`)

### Сложность внедрения
- **Время**: 8-12 часов
- **Файлы**: `AnimatedSticker.tsx`, новый `animation-parser.worker.ts`, `vite.config.ts` для workers
- **Тестирование**: Проверка на mobile Safari и старых Android

### Рекомендация
⚠️ **Не внедрять сейчас**: Offscreen Canvas рискованно для mobile-first приложения. Можно начать с парсинга JSON в Worker для анимаций >100KB.

---

## Стратегия C: Кеширование (In-Memory + IndexedDB + LottieCache)

### Что делает
**Приоритет**: Фаза 1 и Фаза 2

- **LRU in-memory кеш**: Кеш с лимитом 50-100 анимаций, TTL 30 минут
- **IndexedDB для persistent cache**: Сохранение анимаций на диск для offline-доступа
- **Service Worker кеширование**: Кеширование на уровне сети (Cache API)
- **Smart invalidation**: Удаление старых анимаций при нехватке памяти
- **Проактивный кеш**: Prefetch популярных стикеров в IndexedDB

### Архитектура
```typescript
// LRU кеш с TTL
class LRUAnimationCache {
  private cache = new Map<string, {data: any, timestamp: number}>();
  private maxSize = 100;
  private ttl = 30 * 60 * 1000; // 30 минут
  
  get(fileId: string): any {
    const item = this.cache.get(fileId);
    if (!item) return null;
    
    // Проверка TTL
    if (Date.now() - item.timestamp > this.ttl) {
      this.cache.delete(fileId);
      return null;
    }
    
    // Обновление порядка (LRU)
    this.cache.delete(fileId);
    this.cache.set(fileId, item);
    return item.data;
  }
  
  set(fileId: string, data: any): void {
    // Удаление старых если переполнение
    if (this.cache.size >= this.maxSize) {
      const firstKey = this.cache.keys().next().value;
      this.cache.delete(firstKey);
    }
    
    this.cache.set(fileId, {data, timestamp: Date.now()});
  }
}

// IndexedDB для persistence
const db = await openDB('animations', 1, {
  upgrade(db) {
    db.createObjectStore('cache');
  }
});

await db.put('cache', animationData, fileId);
const cached = await db.get('cache', fileId);
```

### Прирост производительности

| Метрика | До | После | Прирост |
|---------|-----|-------|---------|
| **TTFI (повторный визит)** | 2-4s | 0.2-0.5s | **85%** |
| **Offline доступ** | ❌ | ✅ | **∞** |
| **Сетевой трафик** | 100% | 20-40% | **60-80%** |
| **Память (peak)** | 300MB | 180MB | **40%** |

### Риски
- ⚠️ **Средний**: IndexedDB ограничения по размеру (5-50% диска, зависит от устройства)
- ⚠️ **Низкий**: Service Worker кеш требует регистрацию и версионирование
- ⚠️ **Низкий**: Нужна стратегия очистки кеша (когда диск переполняется)
- ✅ **Низкий**: LRU просто реализовать

### Можно внедрить БЕЗ изменений backend
- ✅ **LRU in-memory кеш** (100% frontend, замена текущего Map)
- ✅ **IndexedDB persistence** (требуется библиотека `idb` или нативный API)
- ✅ **Service Worker кеш** (требуется SW файл, но не backend изменения)
- ✅ **TTL и инвалидация** (логика на клиенте)

### Сложность внедрения
- **Время**: 6-8 часов
- **Файлы**: `AnimatedSticker.tsx`, новый `LRUAnimationCache.ts`, `service-worker.ts`, `idb` в `package.json`
- **Тестирование**: Проверка очистки кеша, размеров IndexedDB, offline режим

---

## Стратегия D: Уменьшение нагрузки на DOM (Batch Render + Условный рендеринг)

### Что делает
**Приоритет**: Фаза 1

- **Условный рендеринг только активного стикера**: Не монтировать Lottie для неактивных preview стикеров
- **IntersectionObserver пауза**: Остановка анимаций вне viewport
- **Batch DOM updates**: Группировка изменений DOM через `requestAnimationFrame`
- **Virtualized PackCard**: Отключать анимации в невидимых карточках
- **Debounce рендеринга**: Задержка рендера при быстрой прокрутке

### Архитектура
```typescript
// PackCard.tsx - условный рендеринг
{activeStickerIndex === index && (
  <AnimatedSticker fileId={sticker.fileId} ... />
)}

// Prefetch следующего стикера без монтирования
useEffect(() => {
  const nextIdx = (activeStickerIndex + 1) % previewStickers.length;
  const nextSticker = previewStickers[nextIdx];
  if (nextSticker) {
    // Загружаем JSON, но НЕ рендерим
    animationLoader.load(nextSticker.fileId, nextSticker.url, 0);
  }
}, [activeStickerIndex]);

// AnimatedSticker.tsx - IntersectionObserver пауза
useEffect(() => {
  if (!animationRef.current) return;
  
  const observer = new IntersectionObserver(([entry]) => {
    if (!entry.isIntersecting) {
      animationRef.current.pause(); // Пауза невидимых
    } else {
      animationRef.current.play(); // Возобновление
    }
  }, {threshold: 0.1});
  
  observer.observe(containerRef.current);
  return () => observer.disconnect();
}, [animationRef]);

// Batch DOM updates
const batchUpdate = (() => {
  let pending = [];
  return (update: Function) => {
    pending.push(update);
    requestAnimationFrame(() => {
      pending.forEach(fn => fn());
      pending = [];
    });
  };
})();
```

### Прирост производительности

| Метрика | До | После | Прирост |
|---------|-----|-------|---------|
| **FPS** | 30-45 | 50-60 | **35%** |
| **Активных Lottie** | 40+ | 4-8 | **80%** |
| **DOM nodes** | 2000+ | 500-800 | **60%** |
| **CPU idle** | 20% | 60% | **3x** |
| **Memory (peak)** | 300MB | 120MB | **60%** |

### Риски
- ✅ **Низкий**: Использует стандартные React паттерны и Web API
- ⚠️ **Низкий**: Нужно тестировать IntersectionObserver на всех устройствах
- ⚠️ **Низкий**: Возможна задержка при переключении стикеров (если prefetch не работает)

### Можно внедрить БЕЗ изменений backend
- ✅ **Условный рендеринг** (100% React, `{condition && <Component />}`)
- ✅ **IntersectionObserver** (нативный Web API, нет зависимостей)
- ✅ **Batch updates** (стандартный паттерн React)
- ✅ **Prefetch без монтирования** (загрузка JSON, но не Lottie компонент)

### Сложность внедрения
- **Время**: 3-5 часов
- **Файлы**: `PackCard.tsx`, `AnimatedSticker.tsx`, `useStickerRotation.ts`
- **Тестирование**: Проверка плавности переключения стикеров, пауза при прокрутке

---

## Стратегия E: Замена Lottie на Blitz Preview (Sprite Sheet / Video / Static)

### Что делает
**Приоритет**: Фаза 4 (альтернатива Lottie)

- **Sprite sheet для preview**: Генерация sprite sheet с 10-20 кадрами из Lottie
- **Video fallback**: Видео файлы для сложных анимаций (если sprite >500KB)
- **Static placeholder**: Статичное изображение первого кадра до взаимодействия
- **Progressive enhancement**: Lottie только при hover/активации
- **WebP/AVIF поддержка**: Современные форматы для лучшего сжатия

### Архитектура
```typescript
// PackCard.tsx - Progressive preview
const [previewMode, setPreviewMode] = useState<'sprite' | 'lottie'>('sprite');

// Sprite sheet анимация через CSS
<div 
  className="sprite-animation"
  style={{
    backgroundImage: `url(${sticker.spriteSheet})`,
    animation: `sprite-animate ${duration}s steps(${frames}) infinite`
  }}
/>

// Переключение на Lottie только при hover
<div 
  onMouseEnter={() => setPreviewMode('lottie')}
  onMouseLeave={() => setPreviewMode('sprite')}
>
  {previewMode === 'lottie' ? (
    <AnimatedSticker fileId={sticker.fileId} />
  ) : (
    <SpritePreview spriteUrl={sticker.spriteUrl} />
  )}
</div>

// CSS анимация sprite
@keyframes sprite-animate {
  from { background-position: 0 0; }
  to { background-position: -${totalWidth}px 0; }
}
```

### Требования Backend
⚠️ **Нужны изменения backend** для генерации sprite sheets:
- Экспорт фреймов из Lottie в PNG/JPG
- Сборка sprite sheet (через canvas или imagemagick)
- Оптимизация sprite (WebP, AVIF)
- CDN для sprite files

### Прирост производительности (если backend поддерживает)

| Метрика | До (Lottie) | После (Sprite) | Прирост |
|---------|-------------|----------------|---------|
| **TTFI** | 2-4s | 0.5-1s | **75%** |
| **FPS** | 30-45 | 55-60 | **50%** |
| **Bundle size** | 200KB+ | 50KB | **75%** |
| **Memory** | 200MB | 50MB | **75%** |
| **CPU** | Высокий | Низкий | **60%** |

### Что можно внедрить БЕЗ изменений backend
- ✅ **Static placeholder**: Показывать первое изображение из Lottie
- ✅ **Progressive enhancement**: Lottie только при hover
- ❌ **Sprite sheets**: Нужна генерация на backend
- ❌ **Video**: Нужны видео файлы

### Риски
- 🔴 **КРИТИЧЕСКИЙ**: Требует генерацию sprite sheets на backend
- ⚠️ **Средний**: Sprite sheets могут быть больше, чем Lottie JSON (если >20 кадров)
- ⚠️ **Низкий**: Нужна fallback логика для старых браузеров (автоплей видео в IE)
- ⚠️ **Средний**: Время разработки sprite pipeline на backend (2-3 недели)

### Сложность внедрения
- **Время**: 10-15 часов (frontend) + 2-3 недели (backend)
- **Файлы**: Новый компонент `SpritePreview.tsx`, CSS анимации, backend pipeline
- **Тестирование**: Проверка качества sprite, размер файлов, поддержка браузеров

### Рекомендация
❌ **Не внедрять сейчас**: Стратегия E требует изменений backend. Оставьте для будущих итераций после оптимизации frontend (стратегии A, C, D).

---

## Сравнительная таблица стратегий

| Стратегия | TTFI | FPS | Память | Backend | Риск | Время |
|-----------|------|-----|--------|---------|------|-------|
| **A: Загрузка** | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ❌ | ✅ Низкий | 4-6ч |
| **B: Декодирование** | ⭐ | ⭐⭐⭐ | ⭐⭐ | ❌ | ⚠️ Средний | 8-12ч |
| **C: Кеширование** | ⭐⭐⭐ | ⭐ | ⭐⭐⭐ | ❌ | ✅ Низкий | 6-8ч |
| **D: DOM** | ⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ❌ | ✅ Низкий | 3-5ч |
| **E: Blitz Preview** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ✅ Да | 🔴 Высокий | 2-3 нед |

---

## Рекомендуемый план внедрения

### Неделя 1 (быстрые победы)
- **Стратегия D**: Условный рендеринг + IntersectionObserver (3-5ч)
- **Стратегия A**: Prefetch следующего стикера (2ч)
- **Ожидаемый эффект**: FPS: 45-55, Память: 150MB, DOM nodes: -60%

### Неделя 2 (кеширование)
- **Стратегия C**: LRU in-memory кеш (3ч)
- **Стратегия A (продолжение)**: Batching + дедупликация (4ч)
- **Ожидаемый эффект**: TTFI: 1-2s, Сетевой трафик: -70%

### Неделя 3 (опционально)
- **Стратегия B**: Web Worker парсинг для анимаций >100KB (6ч)
- **Стратегия C (продолжение)**: IndexedDB persistence (4ч)
- **Ожидаемый эффект**: FPS: 55-60, Offline доступ: ✅

### Неделя 4+ (долгосрочные)
- **Стратегия E**: Backend pipeline для sprite sheets (если нужен radical improvement)
- **Мониторинг**: PerformanceObserver метрики
- **Адаптивное качество**: Device detection + adaptive FPS

---

## Критерии выбора стратегий

### Выберите **D + A** если:
- ✅ Нужен быстрый результат (1 неделя)
- ✅ Нет времени на сложные изменения
- ✅ Приоритет: FPS и память

### Выберите **D + A + C** если:
- ✅ Нужен полный набор оптимизаций (2 недели)
- ✅ Важен TTFI и сетевой трафик
- ✅ Пользователи часто возвращаются

### Выберите **все кроме E** если:
- ✅ Долгосрочная оптимизация (3+ недели)
- ✅ Терпимы к сложности
- ✅ Нужен offline-доступ

### НЕ выбирайте **E** сейчас если:
- ❌ Нет ресурсов на backend изменения
- ❌ Нужен быстрый результат
- ❌ Backend команда занята






