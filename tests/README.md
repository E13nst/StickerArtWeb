# Автотесты для StickerArtWeb

## Структура тестов

- `gallery.spec.ts` - Базовые тесты галереи
- `gallery-benchmark.spec.ts` - Бенчмарк производительности галереи
- `performance.spec.ts` - Тесты производительности
- `stickerset-actions.spec.ts` - **Интеграционные тесты действий со стикерсетами (блокировка/разблокировка)**

## Запуск тестов

### Подготовка

1. Установите зависимости:
```bash
npm install
```

2. Установите браузеры Playwright:
```bash
npx playwright install
```

### Авторизация

Тесты требуют авторизации через Telegram. Установите переменную окружения `TELEGRAM_INIT_DATA`:

#### Вариант 1: Через .env файл (рекомендуется)

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` и добавьте ваш `TELEGRAM_INIT_DATA`:
```bash
TELEGRAM_INIT_DATA="user=%7B%22id%22%3A123456789%2C%22first_name%22%3A%22..."
```

**Как получить TELEGRAM_INIT_DATA:**
1. Откройте Mini App в браузере
2. Откройте DevTools (F12) → Network
3. Найдите любой запрос к API
4. Скопируйте значение заголовка `X-Telegram-Init-Data`

#### Вариант 2: Через командную строку

**Linux/Mac:**
```bash
export TELEGRAM_INIT_DATA="user=%7B%22id%22%3A123456789%2C%22first_name%22%3A%22..."
npm run test:actions
```

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_INIT_DATA="user=%7B%22id%22%3A123456789%2C%22first_name%22%3A%22..."
npm run test:actions
```

**Windows (CMD):**
```cmd
set TELEGRAM_INIT_DATA=user=%7B%22id%22%3A123456789%2C%22first_name%22%3A%22...
npm run test:actions
```

⚠️ **ВАЖНО:** Не коммитьте `.env` файл с реальными данными в репозиторий!

### Команды запуска

#### Все тесты
```bash
npm run test
```

#### Тесты блокировки/разблокировки
```bash
npm run test:actions
```

#### Бенчмарк галереи
```bash
npm run test:benchmark
```

#### С UI (браузер видим)
```bash
npm run test:ui
```

#### Конкретный тест
```bash
npx playwright test tests/stickerset-actions.spec.ts
```

#### С отчетом
```bash
npm run test
npx playwright show-report
```

## Тест блокировки/разблокировки

### Что тестирует

Файл: `stickerset-actions.spec.ts`

**Тест 1: Базовая блокировка и разблокировка**
1. Открывает галерею
2. Открывает детальный просмотр любого стикерсета
3. Блокирует стикерсет через кнопку "🚫"
4. Проверяет, что кнопка изменилась на "🔄" (Разблокировать)
5. Разблокирует стикерсет
6. Проверяет, что кнопка вернулась к "🚫" (Заблокировать)

**Тест 2: Полный цикл**
- Выполняет несколько циклов блокировки/разблокировки
- Проверяет корректность кнопок на каждом шаге

### Ожидаемые результаты

✅ **После блокировки:**
- Кнопка "Заблокировать" (🚫) должна исчезнуть
- Кнопка "Разблокировать" (🔄) должна появиться
- Alert "Набор заблокирован" должен отображаться

✅ **После разблокировки:**
- Кнопка "Разблокировать" (🔄) должна исчезнуть
- Кнопка "Заблокировать" (🚫) должна появиться обратно
- Alert "Набор заблокирован" должен исчезнуть

### Пример вывода

```
🚀 Начало теста блокировки/разблокировки стикерсета...

📄 Переход в галерею...
✅ Галерея загружена
✅ Найдено 20 карточек стикерсетов
🖱️  Открываем детальный просмотр первого стикерсета...
✅ Модальное окно с деталями открыто

📊 Проверка начального состояния кнопок действий...
  - Кнопка "Заблокировать" (🚫): Видима
  - Кнопка "Разблокировать" (🔄): Скрыта
  📌 Начальное состояние: НЕ ЗАБЛОКИРОВАН

🚫 ТЕСТ: Блокировка стикерсета...
  🖱️  Клик по кнопке "Заблокировать"...
  ✅ Диалог блокировки открылся
  ✍️  Введена причина блокировки
  🖱️  Клик по кнопке подтверждения...
  ⏳ Ожидание обновления UI...
  ✅ Диалог закрылся
  🔍 Проверка обновления кнопок после блокировки...
  ✅ Кнопка "Заблокировать" (🚫) скрыта
  ✅ Кнопка "Разблокировать" (🔄) появилась
  ✅ Alert о блокировке: Видим

🔄 ТЕСТ: Разблокировка стикерсета...
  🖱️  Клик по кнопке "Разблокировать"...
  ✅ Диалог разблокировки открылся
  🖱️  Клик по кнопке подтверждения...
  ⏳ Ожидание обновления UI...
  ✅ Диалог закрылся
  🔍 Проверка обновления кнопок после разблокировки...
  ✅ Кнопка "Разблокировать" (🔄) скрыта
  ✅ Кнопка "Заблокировать" (🚫) появилась обратно
  ✅ Alert о блокировке: Не видим (ожидается)

✅ ТЕСТ ЗАВЕРШЕН УСПЕШНО
═══════════════════════════════════════════════════════════════════════════════
📊 Результат: Кнопки действий корректно обновляются после блокировки/разблокировки
═══════════════════════════════════════════════════════════════════════════════
```

## Отладка

### Просмотр в браузере
```bash
npx playwright test tests/stickerset-actions.spec.ts --headed --slowMo=1000
```

### Режим отладки
```bash
npx playwright test tests/stickerset-actions.spec.ts --debug
```

### Скриншоты при ошибках
Playwright автоматически делает скриншоты при падении тестов. Они сохраняются в `test-results/`.

### Trace при ошибках
```bash
npx playwright test --trace on
npx playwright show-trace trace.zip
```

## Требования

- Node.js 18+
- Playwright 1.40+
- Dev-сервер должен быть запущен на `http://localhost:3000` или переменная `BASE_URL` настроена
- Права администратора для тестирования блокировки

