# Руководство для AI агентов по работе с проектом

## 🎯 Ключевые архитектурные принципы

### 1. **Приоритетная загрузка медиа (LoadPriority)**

Проект использует систему приоритетов для оптимальной загрузки ресурсов:

```typescript
enum LoadPriority {
  TIER_0_MODAL = 5,            // Наивысший: модальные окна
  TIER_1_FIRST_6_PACKS = 4,    // Первые 6 паков на экране
  TIER_2_FIRST_IMAGE = 3,      // Первое изображение каждого пака
  TIER_3_ADDITIONAL = 2,       // Дополнительные изображения
  TIER_4_BACKGROUND = 1        // Фоновая загрузка
}
```

**Правило:** Всегда используй `imageLoader.loadImage()` с приоритетом вместо прямого `<img src>`.

### 2. **Универсальная предзагрузка (Universal Prefetch)**

Для медиа-контента используй `prefetchSticker()` вместо специфичных функций:

```typescript
// ✅ Правильно - определяет тип автоматически
prefetchSticker(fileId, url)  // Lottie JSON → animationCache, WebP/PNG/GIF → браузерный кеш

// ❌ Неправильно - зависит от типа
prefetchAnimation(fileId, url)  // Только для JSON
imageLoader.loadImage(...)      // Только для изображений
```

**Принцип:** "Один API для всех форматов".

### 3. **Кеширование с маркировкой источника**

Отслеживай происхождение ресурсов для умной очистки кеша:

```typescript
// Маркировать галерейные анимации
markAsGalleryAnimation(fileId);

// Очищать только модальные анимации
clearNonGalleryAnimations();  // Сохраняет галерею в кеше
```

**Правило:** Модальные окна очищают свой кеш при закрытии, галерея остается.

### 4. **Условная анимация через CSS классы**

Управление анимацией через глобальный класс `modal-open`:

```typescript
// Модальное окно открыто
document.body.classList.add('modal-open');     // Пауза всех анимаций

// Модальное окно закрыто
document.body.classList.remove('modal-open');  // Возобновление
```

**Компоненты должны проверять:** `document.body.classList.contains('modal-open')`

### 5. **Проверка готовности перед каруселью**

Карусель должна ждать загрузки следующего ресурса:

```typescript
// ✅ Правильно
const isStickerReady = (fileId, url, isAnimated) => {
  return isAnimated 
    ? animationCache.has(fileId)  // JSON кеш
    : imageCache.has(fileId);     // Image кеш
};

await waitForStickerReady(fileId, url, isAnimated);
scheduleRotation();  // Только после готовности

// ❌ Неправильно
scheduleRotation();  // Сразу, без проверки
```

### 6. **Модальные окна без скролла страницы**

Предотвращение подскоков при открытии модалки:

```typescript
// ✅ Правильно
useEffect(() => {
  if (open) {
    document.body.style.overflow = 'hidden';
    document.body.classList.add('modal-open');
    return () => {
      document.body.style.overflow = '';
      document.body.classList.remove('modal-open');
    };
  }
}, [open]);

// ❌ Неправильно
useEffect(() => {
  if (open) {
    const scrollY = window.scrollY;
    document.body.style.position = 'fixed';
    document.body.style.top = `-${scrollY}px`;  // Вызывает визуальные артефакты
  }
}, [open]);
```

### 7. **Optimistic UI для лайков**

Оптимистичное обновление UI с откатом при ошибке:

```typescript
// Zustand store обеспечивает:
1. Мгновенное UI обновление
2. Debounced API call (300ms)
3. Автоматический откат при ошибке
4. Защита от race conditions

// Не делай вручную debounce/retry - доверь store
```

### 8. **Dependency arrays в useCallback/useMemo**

Включай только реальные зависимости:

```typescript
// ✅ Правильно
const preloadStickers = useCallback((stickers) => {
  stickers.forEach(sticker => prefetchSticker(sticker.fileId, sticker.url));
}, [isModal]);  // Только внешние зависимости

// ❌ Неправильно
const preloadStickers = useCallback((stickers) => {
  stickers.forEach(sticker => prefetchSticker(sticker.fileId, sticker.url));
}, [stickers]);  // Массив создается каждый раз → бесконечный пересоздание
```

---

## 🚫 Антипаттерны

### ❌ "Дедупликация через if/else"
```typescript
// Не делай так:
if (isAnimated) {
  prefetchAnimation(fileId, url);
} else {
  imageLoader.loadImage(fileId, url);
}

// Делай так:
prefetchSticker(fileId, url);  // Сам определит тип
```

### ❌ "Прямые DOM манипуляции"
```typescript
// Не делай так:
document.body.style.position = 'fixed';

// Делай так:
document.body.style.overflow = 'hidden';
document.body.classList.add('modal-open');
```

### ❌ "Блокирующая загрузка"
```typescript
// Не делай так:
const stickers = await loadAllStickers();  // Блокирует UI

// Делай так:
loadStickers().then(stickers => { ... });  // Асинхронно
```

---

## 🔧 Проверочный список при изменениях

- [ ] Используется ли `LoadPriority` для всех медиа?
- [ ] Есть ли проверка готовности перед каруселью?
- [ ] Паузится ли анимация при `modal-open`?
- [ ] Используется ли `prefetchSticker` для предзагрузки?
- [ ] Добавлена ли маркировка для очистки кеша?
- [ ] Правильно ли настроены dependency arrays?
- [ ] Нет ли прямых DOM манипуляций с `position: fixed`?
- [ ] Есть ли защита от race conditions в async операциях?

---

## 📁 Важные файлы

### Утилиты
- `miniapp/src/utils/imageLoader.ts` - приоритетная очередь загрузки
- `miniapp/src/utils/animationLoader.ts` - кеш Lottie и универсальная предзагрузка
- `miniapp/src/utils/stickerUtils.ts` - URL построение для стикеров

### Хуки
- `miniapp/src/hooks/useStickerRotation.ts` - карусель с проверкой готовности
- `miniapp/src/hooks/useNearVisible.ts` - IntersectionObserver обертка

### Компоненты
- `miniapp/src/components/AnimatedSticker.tsx` - универсальный рендер (Lottie/Image)
- `miniapp/src/components/ModalBackdrop.tsx` - управление модалками
- `miniapp/src/components/PackCard.tsx` - карточка пакa с каруселью

### Store
- `miniapp/src/store/useLikesStore.ts` - optimistic UI с debounce и откатом

---

## 💡 Философия проекта

**1. Минимализм** - избегай условной логики, используй универсальные API  
**2. Производительность** - приоритеты загрузки важнее красивых анимаций  
**3. UX first** - оптимистичные обновления, мгновенный отклик  
**4. Умный кеш** - маркировка источника для избирательной очистки  
**5. Graceful degradation** - fallback всегда должен быть

---

**Дата создания:** 2025-01-XX  
**Последнее обновление:** 2025-01-XX






