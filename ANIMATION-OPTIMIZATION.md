# Оптимизация рендеринга анимаций и видео

## 🎯 Проблема

При прокрутке галереи с 40-60 стикерсетами (с анимациями) телефон сильно нагревается, потому что все анимации и видео продолжают рендериться даже вне viewport.

## ✅ Совместимость с бенчмарком

**Оптимизация НЕ ломает бенчмарк!** 

Бенчмарк проверяет наличие элементов через `querySelector`, который работает даже если элемент скрыт через `display: none`. Элементы остаются в DOM, просто не рендерятся, что экономит ресурсы.

**Добавлена статистика рендеринга в бенчмарк:**
- После загрузки всех карточек бенчмарк показывает:
  - Сколько анимаций активно рендерится
  - Сколько на паузе (не рендерится)
  - Сколько видео воспроизводится
  - Эффективность оптимизации (% элементов на паузе)

## ✅ Решение

Реализована полная остановка рендеринга анимаций и видео вне viewport:

1. **AnimatedSticker** - полностью останавливает рендеринг Lottie анимаций вне viewport
2. **PackCard** - паузит видео вне viewport
3. **animationMonitor** - утилита для диагностики активных анимаций

## 🔍 Как проверить работу оптимизации

### В браузере (DevTools):

1. Откройте консоль разработчика
2. Прокрутите галерею вниз (2-3 страницы)
3. Выполните в консоли:

```javascript
// Получить статистику активных анимаций
window.animationMonitor.getStats()

// Логировать статистику
window.animationMonitor.logStats()

// Принудительно паузить все вне viewport
window.animationMonitor.pauseOutOfViewport()
```

### Ожидаемые результаты:

- **activeAnimations**: должно быть только для видимых элементов (обычно 6-12)
- **pausedAnimations**: должно быть для всех элементов вне viewport
- **activeVideos**: должно быть только для видимых видео
- **pausedVideos**: должно быть для всех видео вне viewport

### Визуальная проверка:

1. Откройте DevTools → Performance
2. Запустите запись
3. Прокрутите галерею вниз
4. Остановите запись
5. Проверьте:
   - **CPU usage** должен быть ниже
   - **Frame rate** должен быть стабильным (60 FPS)
   - Меньше активности для элементов вне viewport

### На телефоне:

1. Откройте мини-апп в Telegram
2. Прокрутите 2-3 страницы галереи
3. Проверьте:
   - **Температура телефона** должна быть ниже
   - **Батарея** должна разряжаться медленнее
   - **Плавность прокрутки** должна быть лучше

## 🔧 Технические детали

### AnimatedSticker:

- Использует `IntersectionObserver` для отслеживания видимости
- При выходе из viewport (далеко, >300px):
  - Паузит анимацию через `animationRef.current.pause()`
  - Скрывает через `visibility: 'hidden'` (останавливает рендеринг, но элемент остается в DOM)
  - Это предотвращает пустые карточки, но экономит CPU/GPU
- При входе в viewport:
  - Показывает через `visibility: 'visible'`
  - Возобновляет анимацию через `animationRef.current.play()`

### PackCard (видео):

- Использует `IntersectionObserver` для отслеживания видимости видео
- При выходе из viewport:
  - Паузит видео через `video.pause()`
- При входе в viewport:
  - Возобновляет воспроизведение через `video.play()`

### Параметры IntersectionObserver:

- `threshold: 0.1` - срабатывает когда 10% элемента видно
- `rootMargin: '100px'` - останавливает за 100px до выхода из viewport

## 📊 Мониторинг

Утилита `animationMonitor` доступна в консоли:

```javascript
// Получить текущую статистику
const stats = window.animationMonitor.getStats();
console.log('Активных анимаций:', stats.activeAnimations);
console.log('На паузе:', stats.pausedAnimations);
console.log('Активных видео:', stats.activeVideos);
console.log('На паузе:', stats.pausedVideos);
```

## 🚀 Дальнейшие оптимизации

Если проблема сохраняется, можно:

1. **Увеличить rootMargin** для более ранней остановки (например, до 200px)
2. **Добавить debounce** для IntersectionObserver
3. **Использовать `will-change: auto`** для элементов вне viewport
4. **Lazy loading** - загружать анимации только при приближении к viewport

## ⚠️ Известные ограничения

- Lottie может продолжать использовать память даже при `display: none`
- Видео могут продолжать буферизацию даже на паузе
- IntersectionObserver может не срабатывать при очень быстрой прокрутке

## 🔗 Связанные файлы

- `miniapp/src/components/AnimatedSticker.tsx` - компонент анимации
- `miniapp/src/components/PackCard.tsx` - карточка стикерсета
- `miniapp/src/utils/animationMonitor.ts` - утилита мониторинга

