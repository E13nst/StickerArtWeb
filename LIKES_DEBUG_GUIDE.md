# 🐛 Руководство по отладке лайков

## 🎯 Что было исправлено

### ❌ Проблема: API не возвращал `isLiked`
**Решение:** Добавлено поле `isLiked?: boolean` в `StickerSetResponse`

### ❌ Проблема: `initializeLikes` игнорировал данные от API
**Решение:** Приоритет данных:
1. Если API вернул `isLiked` → используем его ✅
2. Если нет → берем из локального store
3. Если и там нет → `false` по умолчанию

### ✅ Добавлено детальное логирование

## 🔍 Как отладить проблему

### Шаг 1: Откройте консоль браузера
```
F12 → Console
```

### Шаг 2: Найдите логи при загрузке галереи
Должны появиться:
```
🔍 DEBUG: Инициализация лайков из API: [{id, title, likes, isLiked}, ...]
🔍 DEBUG initializeLikes: Получено стикерсетов: 20
🔍 DEBUG: Стикерсет 123: {apiIsLiked: true, storeIsLiked: undefined, finalIsLiked: true, likes: 42}
✅ DEBUG: Инициализировано 20 лайков
```

### Шаг 3: Проверьте что возвращает API
```
🔍 DEBUG: Инициализация лайков из API:
[
  {
    id: 123,
    title: "Cute Cats",
    likes: 42,          ← Общее количество лайков
    isLiked: true       ← Лайкнул ли ТЕКУЩИЙ пользователь (ВАЖНО!)
  },
  ...
]
```

### Шаг 4: Проверьте отображение
При рендере каждой карточки должен быть лог:
```
🔍 DEBUG InteractiveLikeCount [123]: {packId: "123", isLiked: true, likesCount: 42}
```

## 📋 Чек-лист проблем

### ✅ Проблема 1: API не возвращает `isLiked`
**Симптом:** В логах `apiIsLiked: undefined` для всех стикеров

**Причина:** Бэкенд не добавляет поле `isLiked` в response

**Решение на бэкенде:**
```java
// Spring Boot пример
@GetMapping("/stickersets")
public Page<StickerSetDto> getStickerSets(
    @RequestHeader("X-Telegram-Init-Data") String initData,
    Pageable pageable
) {
    Long userId = extractUserIdFromInitData(initData);
    
    return stickerSetService.findAll(pageable)
        .map(stickerSet -> {
            StickerSetDto dto = mapper.toDto(stickerSet);
            
            // ✅ ВАЖНО: Добавляем поле isLiked!
            if (userId != null) {
                dto.setIsLiked(likeService.isLiked(userId, stickerSet.getId()));
            }
            
            return dto;
        });
}
```

**Временный workaround (frontend):**
Если API не может быстро добавить `isLiked`, используется fallback к локальному store.

---

### ✅ Проблема 2: `likesCount` всегда 0
**Симптом:** В логах `likes: 0` для всех стикеров

**Причина:** API не возвращает поле `likes`

**Проверка API:**
```bash
curl -X GET "https://api.example.com/stickersets" \
  -H "X-Telegram-Init-Data: user=..." \
  | jq '.content[0].likes'
```

Должно вернуть число, а не `null`!

**Решение на бэкенде:**
```java
// Добавить аннотацию @JsonProperty или убедиться что геттер есть
public class StickerSetDto {
    private Long id;
    private String title;
    private Integer likes;  // ✅ Должно быть в response!
    
    @JsonProperty("isLiked")
    private Boolean isLiked;  // ✅ И это тоже!
    
    // getters/setters
}
```

---

### ✅ Проблема 3: Клик на лайк не работает
**Симптом:** Клик есть, но UI не меняется

**Проверка в console:**
1. Найдите лог при клике:
```
⚠️ Rate limit: слишком частые запросы для 123. Подождите 1000ms
```
→ Вы кликаете слишком быстро, подождите 1 секунду

2. Или:
```
✅ Лайк синхронизирован с сервером для 123: {success: true, likes: 43}
```
→ Запрос прошел успешно

3. Или:
```
❌ Ошибка синхронизации лайка для 123: Error: Не удалось изменить лайк
```
→ Проблема с API

**Проверка в Network tab:**
- Должен быть запрос: `PUT /api/likes/stickersets/123/toggle`
- Status: `200 OK`
- Response: `{success: true, likes: 43}`

---

### ✅ Проблема 4: Лайк ставится и сразу откатывается
**Симптом:** Иконка краснеет, потом возвращается обратно

**Причина:** Rollback при ошибке API

**Проверка в console:**
```
❌ Ошибка синхронизации лайка для 123: Error: ...
```

**Возможные причины:**
1. **401 Unauthorized** → Нет авторизации (X-Telegram-Init-Data header)
2. **403 Forbidden** → Пользователь не имеет прав
3. **404 Not Found** → Неправильный endpoint или ID
4. **429 Too Many Requests** → Rate limit на сервере
5. **500 Internal Server Error** → Ошибка на бэкенде

---

### ✅ Проблема 5: Разные лайки в галерее и модальном окне
**Симптом:** В галерее лайкнуто, в модалке - нет (или наоборот)

**Причина:** Store не обновляется или используются разные packId

**Проверка:**
```javascript
// В console браузера
useLikesStore.getState().likes
// Должен вернуть объект с лайками
```

Проверьте что packId одинаковый (string):
```
PackCard: packId = "123"
StickerSetDetail: packId = "123"
```

Если один number, другой string → проблема!

---

## 🛠️ Инструменты отладки

### 1. Zustand DevTools
Установите расширение:
```bash
npm install --save-dev @redux-devtools/extension
```

Посмотрите состояние store в реальном времени.

### 2. React DevTools
Найдите компонент `InteractiveLikeCount` и посмотрите props:
- `packId`
- `likesCount`
- `isLiked`

### 3. Network Tab
Фильтр: `likes`

Смотрите все запросы к `/api/likes/...`:
- Статус ответа
- Время ответа
- Payload
- Response

---

## ✅ Итоговый чек-лист

Проверьте по порядку:

- [ ] API возвращает `likes` для каждого стикерсета
- [ ] API возвращает `isLiked` для авторизованного пользователя
- [ ] В console появляются логи `🔍 DEBUG: Инициализация лайков`
- [ ] Видны все стикерсеты с их `isLiked` и `likes`
- [ ] При клике появляется лог с анимацией
- [ ] В Network tab запрос `PUT /toggle` возвращает 200
- [ ] После клика UI обновляется (красное/белое сердце)
- [ ] Счетчик лайков увеличивается/уменьшается
- [ ] При повторном клике лайк снимается
- [ ] В модальном окне лайки синхронизированы с галереей

---

## 🚀 Быстрый тест

Откройте консоль и выполните:

```javascript
// 1. Проверьте состояние store
console.log('Store:', useLikesStore.getState().likes);

// 2. Лайкните стикер программно
await useLikesStore.getState().toggleLike('123');

// 3. Проверьте что изменилось
console.log('После лайка:', useLikesStore.getState().likes['123']);
```

Если все работает → проблема в UI/рендеринге
Если не работает → проблема в store/API

---

## 📞 Что делать если ничего не помогло

1. **Очистите localStorage**
```javascript
localStorage.removeItem('likes-storage');
location.reload();
```

2. **Проверьте версию store**
Должна быть версия 3 (константа `STORAGE_VERSION = 3`)

3. **Проверьте что dev сервер запущен**
```bash
cd miniapp && npm run dev
```

4. **Откройте браузер в режиме инкогнито**
Это исключит проблемы с кэшем

5. **Покажите логи из консоли**
Скопируйте все логи с префиксом `🔍 DEBUG` и `❌ Ошибка`

---

## 📝 Формат отчета об ошибке

```
**Описание проблемы:**
Лайки не отображаются корректно

**Что происходит:**
- При клике иконка краснеет, но счетчик не меняется
- После перезагрузки лайки пропадают

**Логи из консоли:**
[Вставьте логи с 🔍 DEBUG и ❌]

**Network tab (запрос /toggle):**
Status: 200
Response: {success: true, likes: 43}

**Версия браузера:**
Chrome 120.0.0.0
```

---

Удачной отладки! 🐛➡️✅

