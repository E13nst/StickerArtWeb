# Технический отчет: Анализ потребления памяти GalleryPage

## Дата анализа
2025-01-27

## Исполнитель
AI Code Assistant

---

## 1. Общее описание функционала GalleryPage

### Назначение экрана
`GalleryPage` — основная страница приложения для просмотра и поиска наборов стикеров (стикерсетов). Экран реализует бесконечную прокрутку с автоматической подгрузкой следующих страниц при приближении к концу списка.

### Как устроен список/лента стикеров
Галерея реализована через компонент `SimpleGallery`, который:
- Отображает стикерсеты в двухколоночной сетке (masonry layout)
- Поддерживает два режима отображения:
  - **Обычный режим**: все элементы рендерятся в DOM (используется до ~50-100 элементов)
  - **Виртуализированный режим**: рендерится только видимая часть списка через `VirtualizedGallery` (включается при большом количестве элементов)

Каждая карточка стикерсета (`PackCard`) отображает:
- Первый стикер из набора (с возможностью ротации)
- Заголовок набора
- Количество лайков
- Вспомогательную информацию (для админов)

### Как реализована пагинация и загрузка данных
Пагинация реализована через механизм **infinite scroll** с использованием `IntersectionObserver`:

1. **Загрузка данных**:
   - При монтировании: загружается первая страница (20 элементов)
   - При скролле: при приближении к `sentinel` элементу (на ~200px до конца) автоматически вызывается `loadMoreStickerSets()`
   - Данные накапливаются в `useStickerStore.stickerSets` через метод `addStickerSets()`

2. **Триггер загрузки**:
   ```typescript
   // SimpleGallery.tsx:244-273
   const observer = new IntersectionObserver(
     (entries) => {
       if (entry.isIntersecting && hasNextPage && !isLoadingMore && onLoadMore) {
         onLoadMore(); // Вызывает fetchStickerSets(currentPage + 1, true)
       }
     },
     { rootMargin: '200px', threshold: 0.1 }
   );
   ```

3. **Управление состоянием**:
   - `currentPage`, `totalPages`, `totalElements` хранятся в `useStickerStore`
   - При каждой подгрузке новые стикерсеты добавляются к существующим через `addStickerSets()`:
     ```typescript
     // useStickerStore.ts:72-78
     addStickerSets: (newStickerSets) => {
       const { stickerSets } = get();
       const existingIds = new Set(stickerSets.map(s => s.id));
       const uniqueNewSets = newStickerSets.filter(s => !existingIds.has(s.id));
       set({ stickerSets: [...stickerSets, ...uniqueNewSets] });
     }
     ```

---

## 2. Архитектура и ключевые компоненты

### Список файлов/модулей

#### UI Компоненты
1. **`miniapp/src/pages/GalleryPage.tsx`** (673 строки)
   - Главный компонент страницы
   - Управление состоянием, фильтрами, поиском
   - Оркестрация загрузки данных и отображения

2. **`miniapp/src/components/SimpleGallery.tsx`** (1028 строк)
   - Компонент галереи с поддержкой виртуализации
   - Управление infinite scroll
   - Выбор между обычным и виртуализированным режимом

3. **`miniapp/src/components/VirtualizedGallery.tsx`** (212 строк)
   - Виртуализированный список (рендерит только видимые элементы)
   - Используется при большом количестве элементов (>50-100)

4. **`miniapp/src/components/PackCard.tsx`** (620 строк)
   - Карточка отдельного стикерсета
   - Загружает и отображает превью стикеров
   - Управляет ротацией стикеров при наведении

5. **`miniapp/src/components/AnimatedPackCard.tsx`** (138 строк)
   - Обертка над `PackCard` с анимацией появления
   - Используется в виртуализированном режиме

#### Загрузка данных
6. **`miniapp/src/store/useStickerStore.ts`** (165 строк)
   - Zustand store для управления состоянием стикерсетов
   - Хранит массив всех загруженных стикерсетов
   - Методы `setStickerSets()` и `addStickerSets()` для накопления данных

7. **`miniapp/src/api/client.ts`**
   - API клиент для запросов к бэкенду
   - Методы `getStickerSets()` и `searchStickerSets()`

#### Работа с изображениями
8. **`miniapp/src/utils/imageLoader.ts`** (1318 строк)
   - Система загрузки изображений/анимаций/видео
   - Очередь загрузки с приоритетами (viewport-based)
   - Дедупликация запросов
   - Управление concurrency (до 30 одновременных загрузок)

9. **`miniapp/src/utils/cacheManager.ts`** (481 строка)
   - Менеджер кэша на основе Cache API браузера
   - Дополнительный синхронный кэш в памяти (`syncCache`)
   - Поддержка типов ресурсов: `image`, `animation`, `video`
   - Лимиты: 200 изображений, 50 анимаций, 30 видео

10. **`miniapp/src/components/AnimatedSticker.tsx`**
    - Компонент для отображения анимированных стикеров (Lottie)
    - Загружает анимации через `imageLoader`

#### Кэширование данных
11. **`miniapp/src/hooks/useSmartCache.ts`** (176 строк)
    - Хук для кэширования данных паков в памяти
    - LRU eviction при превышении лимита (200 элементов)
    - TTL: 5 минут

### Краткое описание ответственности

| Компонент | Ответственность | Проблемы с памятью |
|-----------|----------------|-------------------|
| `GalleryPage` | Оркестрация, управление фильтрами | Нет очистки при unmount |
| `SimpleGallery` | Выбор режима, infinite scroll | Виртуализация включается слишком поздно |
| `VirtualizedGallery` | Рендеринг только видимых элементов | Не используется до ~50-100 элементов |
| `PackCard` | Отображение карточки, загрузка стикеров | Загружает до 3 стикеров для ротации даже для невидимых |
| `useStickerStore` | Хранение всех стикерсетов | Накопление без ограничений при пагинации |
| `imageLoader` | Загрузка медиа-файлов | Большая очередь, высокий concurrency |
| `cacheManager` | Кэш изображений/анимаций/видео | Двойное хранение (Cache API + syncCache Map) |
| `useSmartCache` | Кэш данных паков | LRU с лимитом 200, но может накапливаться |

---

## 3. Модель работы с памятью

### Как хранятся стикеры/изображения в памяти

#### 3.1 Данные стикерсетов
- **Хранение**: Массив `StickerSetResponse[]` в `useStickerStore.stickerSets`
- **Рост при пагинации**: Каждая страница добавляет ~20 элементов
  - После 3 страниц: ~60 элементов
  - После 5 страниц: ~100 элементов
  - **Нет ограничений на размер массива**

#### 3.2 Изображения стикеров
Система двухуровневого кэширования:

1. **Cache API (браузерный)**:
   - Лимиты: 200 изображений, 50 анимаций, 30 видео
   - Управляется браузером, может быть очищен при нехватке памяти
   - Хранится на диске, но декодированные битмапы в памяти

2. **SyncCache (в памяти)**:
   ```typescript
   // cacheManager.ts:53-57
   private syncCache = {
     images: new Map<string, string>(),      // URL strings
     animations: new Map<string, any>(),     // JSON данные (~200KB каждый)
     videos: new Map<string, string>()       // Blob URLs
   };
   ```
   - **Проблема**: Дублирует Cache API полностью в памяти
   - Хранит все закешированные ресурсы синхронно для быстрого доступа
   - **Нет автоматической очистки** при превышении лимитов Cache API

3. **Декодированные битмапы в DOM**:
   - Каждое изображение в `<img>` или `<video>` декодируется браузером
   - Размер зависит от разрешения: ~512x512px × 4 байта = ~1MB на изображение
   - При 60 стикерсетах × 3 стикера на карточку = 180 изображений = ~180MB

#### 3.3 Анимации (Lottie JSON)
- Хранятся в `syncCache.animations` (Map)
- Размер: ~200KB на анимацию
- При 50 анимациях: ~10MB в памяти

#### 3.4 Видео (Blob URLs)
- Хранятся в `syncCache.videos` (Map) как blob URLs
- Blob хранится в памяти браузера
- Размер: ~2MB на видео
- При 30 видео: ~60MB в памяти

### Где и как они загружаются, декодируются, кэшируются

#### Процесс загрузки:
1. **Запрос ресурса** (`PackCard.tsx:72-119`):
   - При монтировании карточки загружается первый стикер
   - Для видимых карточек также загружаются 2-й и 3-й стикеры для ротации

2. **Через imageLoader** (`imageLoader.ts:245-343`):
   - Проверка кэша → добавление в очередь → загрузка с приоритетом
   - Дедупликация запросов по `fileId` и нормализованному URL

3. **Кэширование** (`cacheManager.ts:119-181`):
   - Сохранение в `syncCache` (синхронно)
   - Сохранение в Cache API (асинхронно)

4. **Декодирование**:
   - Браузер автоматически декодирует изображения при установке `src`
   - Декодированные битмапы остаются в памяти до удаления элемента из DOM

### Есть ли виртуализация списка

**Да, но с проблемами**:

1. **Виртуализация включается условно** (`SimpleGallery.tsx:108-142`):
   ```typescript
   const getVirtualizationThreshold = () => {
     const isMobile = window.innerWidth < 768;
     return isMobile ? 50 : 100;
   };
   ```
   - На мобильных: включается при >50 элементов
   - На десктопе: включается при >100 элементов

2. **Проблема**: Решение принимается только один раз при первой загрузке
   - Если начальная загрузка <50 элементов, виртуализация не включится
   - После подгрузки 2-3 страниц количество может вырасти до 60+, но виртуализация не активируется

3. **Виртуализированный режим** (`VirtualizedGallery.tsx`):
   - Рендерит только видимые элементы + overscan (6 элементов)
   - Остальные элементы не в DOM, но данные в `useStickerStore` остаются

---

## 4. Найденные проблемы / подозрения

### 4.1 Накопление данных стикерсетов без ограничений

**Файл**: `miniapp/src/store/useStickerStore.ts:72-78`

**Проблема**:
```typescript
addStickerSets: (newStickerSets) => {
  const { stickerSets } = get();
  const existingIds = new Set(stickerSets.map(s => s.id));
  const uniqueNewSets = newStickerSets.filter(s => !existingIds.has(s.id));
  set({ stickerSets: [...stickerSets, ...uniqueNewSets] }); // ⚠️ Массив растет бесконечно
}
```

**Последствия**:
- После 3 страниц: ~60 стикерсетов × ~5KB (JSON) = ~300KB данных
- После 10 страниц: ~200 стикерсетов × ~5KB = ~1MB данных
- Каждый стикерсет содержит массив `previewStickers` с URL-ами изображений
- При большом количестве страниц массив может достичь сотен элементов

**Вероятность**: 🔴 **ВЫСОКАЯ** — основная причина роста памяти

**Почему это увеличивает потребление памяти**:
- Каждый элемент массива хранится в памяти Zustand store
- При изменении массива создается новый массив (иммутабельность)
- React пересоздает компоненты при изменении массива

---

### 4.2 Отсутствие виртуализации при начальной загрузке

**Файл**: `miniapp/src/components/SimpleGallery.tsx:108-142`

**Проблема**:
```typescript
// Виртуализация определяется только при первой загрузке
if (virtualizationDecisionRef.current === null && packs.length > 0) {
  const virtualizationThreshold = getVirtualizationThreshold();
  virtualizationDecisionRef.current = packs.length > virtualizationThreshold;
}
```

**Последствия**:
- При первой загрузке 20 элементов виртуализация не включается (< 50-100)
- После подгрузки 2-3 страниц количество может вырасти до 60+, но виртуализация уже не активируется
- Все 60+ элементов остаются в DOM одновременно

**Вероятность**: 🔴 **ВЫСОКАЯ** — критично для DOM и декодированных битмапов

**Почему это увеличивает потребление памяти**:
- Все элементы рендерятся в DOM
- Все изображения декодируются браузером в битмапы
- При 60 элементах × 3 изображения × ~1MB = ~180MB только на битмапы

---

### 4.3 Двойное кэширование: Cache API + syncCache Map

**Файл**: `miniapp/src/utils/cacheManager.ts:53-57, 119-181`

**Проблема**:
```typescript
// Два места хранения одних и тех же данных:
// 1. Cache API (браузерный, на диске)
// 2. syncCache Map (в памяти, для быстрого доступа)

private syncCache = {
  images: new Map<string, string>(),
  animations: new Map<string, any>(),    // ⚠️ JSON ~200KB каждый
  videos: new Map<string, string>()
};
```

**Последствия**:
- Все закешированные ресурсы дублируются в памяти через `syncCache`
- При 200 изображениях: 200 × URL string = минимальный overhead
- При 50 анимациях: 50 × 200KB = ~10MB в памяти
- При 30 видео: 30 × blob URL string = минимальный overhead, но blob сам в памяти

**Вероятность**: 🟡 **СРЕДНЯЯ** — проблема усугубляется при большом количестве анимаций

**Почему это увеличивает потребление памяти**:
- Полное дублирование данных Cache API в памяти
- Для анимаций (JSON) это особенно критично (~200KB × количество)

---

### 4.4 Предзагрузка стикеров для ротации на невидимых карточках

**Файл**: `miniapp/src/components/PackCard.tsx:145-186`

**Проблема**:
```typescript
// Загружаем 2-й и 3-й стикеры для ротации независимо от видимости
for (let i = 1; i < Math.min(pack.previewStickers.length, 3); i++) {
  const sticker = pack.previewStickers[i];
  const priority = isInViewport
    ? LoadPriority.TIER_2_NEAR_VIEWPORT
    : LoadPriority.TIER_3_ADDITIONAL; // ⚠️ Все равно загружаем
  
  imageLoader.loadImage(sticker.fileId, sticker.url, priority);
}
```

**Последствия**:
- Для каждой карточки загружаются до 3 стикеров
- При 60 карточках это может быть 180 изображений
- Большинство из них не видны пользователю (ниже viewport)

**Вероятность**: 🟡 **СРЕДНЯЯ** — проблема усугубляется при большом количестве карточек

**Почему это увеличивает потребление памяти**:
- Загрузка изображений, которые пользователь может никогда не увидеть
- Декодирование битмапов для невидимых изображений
- Наполнение кэша неиспользуемыми ресурсами

---

### 4.5 Отсутствие очистки при unmount страницы

**Файл**: `miniapp/src/pages/GalleryPage.tsx`

**Проблема**:
- При уходе со страницы данные в `useStickerStore.stickerSets` не очищаются
- Кэш изображений (`cacheManager.syncCache`) не очищается
- Очередь загрузки (`imageLoader.queue`) не очищается

**Последствия**:
- Данные накапливаются при переходах между страницами
- Кэш растет без ограничений между сессиями

**Вероятность**: 🟢 **НИЗКАЯ** — проблема проявляется при навигации, не при пагинации

---

### 4.6 Высокий concurrency загрузки изображений

**Файл**: `miniapp/src/utils/imageLoader.ts:137-139`

**Проблема**:
```typescript
maxConcurrency: 30, // 🔥 УВЕЛИЧЕНО: до 30 для поддержки 40 карточек одновременно
```

**Последствия**:
- Одновременно может загружаться до 30 изображений
- При загрузке большого изображения (~1MB) это может быть ~30MB в памяти во время загрузки
- Декодированные битмапы добавляются к уже загруженным

**Вероятность**: 🟢 **НИЗКАЯ** — временная проблема во время загрузки

---

## 5. Оценка потребления памяти

### Примерные значения (оценочные, на основе логики)

#### После открытия GalleryPage (1 страница, 20 элементов):
- Данные стикерсетов: 20 × 5KB = ~100KB
- DOM элементы: 20 карточек
- Изображения в DOM: 20 × 1MB (декодированные битмапы) = ~20MB
- Кэш (syncCache): минимальный
- **Итого**: ~20-25MB

#### После пролистывания 2-й страницы (40 элементов):
- Данные стикерсетов: 40 × 5KB = ~200KB
- DOM элементы: 40 карточек (если виртуализация не включена)
- Изображения в DOM: 40 × 1MB = ~40MB
- Кэш (syncCache): ~40 изображений × URL + возможные анимации
- **Итого**: ~40-45MB

#### После пролистывания 3-й страницы (60 элементов):
- Данные стикерсетов: 60 × 5KB = ~300KB
- DOM элементы: 60 карточек (если виртуализация не включена) ⚠️
- Изображения в DOM: 60 × 1MB = ~60MB ⚠️
- Кэш (syncCache): ~60 изображений + возможные анимации (~10MB) = ~10-15MB
- Загруженные стикеры для ротации: ~180 изображений в кэше
- **Итого**: ~70-80MB ⚠️ **КРИТИЧНО**

#### После пролистывания 5-й страницы (100 элементов, без виртуализации):
- Данные стикерсетов: 100 × 5KB = ~500KB
- DOM элементы: 100 карточек ⚠️
- Изображения в DOM: 100 × 1MB = ~100MB ⚠️
- Кэш: ~100-150MB (с анимациями)
- **Итого**: ~200-250MB ⚠️ **ОЧЕНЬ КРИТИЧНО**

### Какие объекты добавляются между шагами:
- **При каждой подгрузке страницы**:
  - +20 элементов в `useStickerStore.stickerSets`
  - +20 карточек в DOM (если виртуализация не включена)
  - +60 изображений загружается (20 × 3 для ротации)
  - +60 декодированных битмапов (если все видны)
  - Рост кэша на ~60 URL строк + возможные анимации

---

## 6. Рекомендации

### Приоритет 1 (КРИТИЧНО) — Немедленно исправить

#### 6.1 Включить виртуализацию всегда при пагинации
**Файл**: `miniapp/src/components/SimpleGallery.tsx`

**Решение**:
```typescript
// Пересматривать решение о виртуализации при каждом изменении packs.length
useEffect(() => {
  const virtualizationThreshold = getVirtualizationThreshold();
  // Включаем виртуализацию если элементов больше порога ИЛИ есть пагинация
  const shouldUse = packs.length > virtualizationThreshold || (onLoadMore && packs.length > 20);
  virtualizationDecisionRef.current = shouldUse;
}, [packs.length, onLoadMore]);
```

**Ожидаемый эффект**: Снижение потребления памяти на 70-80% при большом количестве элементов

---

#### 6.2 Ограничить размер массива stickerSets
**Файл**: `miniapp/src/store/useStickerStore.ts`

**Решение**:
```typescript
addStickerSets: (newStickerSets) => {
  const { stickerSets } = get();
  const existingIds = new Set(stickerSets.map(s => s.id));
  const uniqueNewSets = newStickerSets.filter(s => !existingIds.has(s.id));
  
  // ✅ НОВОЕ: Ограничиваем размер массива (оставляем последние 100)
  const MAX_STICKER_SETS = 100;
  const updated = [...stickerSets, ...uniqueNewSets];
  const trimmed = updated.length > MAX_STICKER_SETS 
    ? updated.slice(-MAX_STICKER_SETS) // Оставляем последние 100
    : updated;
  
  set({ stickerSets: trimmed });
}
```

**Альтернатива** (более сложная): Реализовать скользящее окно с удалением старых элементов из DOM через виртуализацию.

**Ожидаемый эффект**: Ограничение роста данных до ~500KB вместо неограниченного роста

---

### Приоритет 2 (ВЫСОКИЙ) — Исправить в ближайшее время

#### 6.3 Очистка syncCache при превышении лимитов Cache API
**Файл**: `miniapp/src/utils/cacheManager.ts`

**Решение**:
```typescript
private async enforceLimit(type: ResourceType): Promise<void> {
  // ... существующая логика ...
  
  // ✅ НОВОЕ: Синхронизируем syncCache с Cache API
  const map = this.syncCache[`${type}s` as 'images' | 'animations' | 'videos'];
  for (const { request } of toDelete) {
    const urlParts = request.url.split('/');
    const fileId = urlParts[urlParts.length - 1];
    map.delete(fileId); // Удаляем из syncCache тоже
  }
}
```

**Ожидаемый эффект**: Предотвращение дублирования данных сверх лимитов

---

#### 6.4 Отложенная загрузка стикеров для ротации
**Файл**: `miniapp/src/components/PackCard.tsx`

**Решение**:
```typescript
// Загружаем дополнительные стикеры только для видимых карточек
useEffect(() => {
  if (pack.previewStickers.length <= 1) return;
  
  // ✅ НОВОЕ: Загружаем только если карточка видна или близко к viewport
  if (!isInViewport && !isNearViewport) {
    return; // Не загружаем для далеких карточек
  }
  
  // ... остальная логика загрузки ...
}, [pack.id, pack.previewStickers, isInViewport, isNearViewport]);
```

**Ожидаемый эффект**: Снижение загрузок на 50-70% для невидимых карточек

---

### Приоритет 3 (СРЕДНИЙ) — Улучшения для стабильности

#### 6.5 Очистка при unmount GalleryPage
**Файл**: `miniapp/src/pages/GalleryPage.tsx`

**Решение**:
```typescript
useEffect(() => {
  return () => {
    // Очищаем данные при уходе со страницы (опционально)
    // setStickerSets([]); // Раскомментировать если нужно
  };
}, []);
```

**Примечание**: Это может ухудшить UX при возврате на страницу (нужна перезагрузка). Альтернатива: очистка только при явном нажатии "Назад" или через определенное время бездействия.

---

#### 6.6 Оптимизация размера декодированных битмапов
**Решение**: Использовать `decoding="async"` и `loading="lazy"` для изображений:
```typescript
<img
  src={...}
  decoding="async"
  loading="lazy"
  ...
/>
```

**Ожидаемый эффект**: Браузер будет декодировать изображения асинхронно и только при необходимости

---

## 7. План действий

### Фаза 1 (Срочно, 1-2 дня):
1. ✅ Включить виртуализацию всегда при пагинации
2. ✅ Ограничить размер массива stickerSets до 100 элементов

### Фаза 2 (В течение недели):
3. ✅ Синхронизировать очистку syncCache с Cache API
4. ✅ Отложенная загрузка стикеров для ротации

### Фаза 3 (Опционально, для улучшения):
5. ✅ Очистка при unmount (с осторожностью)
6. ✅ Оптимизация декодирования изображений

---

## 8. Список затронутых файлов

### Основные файлы для изменений:
1. `miniapp/src/components/SimpleGallery.tsx` — виртуализация
2. `miniapp/src/store/useStickerStore.ts` — ограничение размера массива
3. `miniapp/src/utils/cacheManager.ts` — синхронизация syncCache
4. `miniapp/src/components/PackCard.tsx` — отложенная загрузка ротации
5. `miniapp/src/pages/GalleryPage.tsx` — очистка при unmount (опционально)

### Файлы для мониторинга:
- `miniapp/src/utils/imageLoader.ts` — логирование статистики очереди
- `miniapp/src/hooks/useSmartCache.ts` — мониторинг размера кэша паков

---

## 9. Дополнительные замечания

### Измерения памяти
Для точных измерений рекомендуется использовать:
- Chrome DevTools → Performance → Memory
- Chrome DevTools → Memory → Heap Snapshot
- Performance API: `performance.memory` (устаревший, но все еще работает в некоторых браузерах)

### Мониторинг в production
Добавить логирование статистики:
```typescript
// В GalleryPage или SimpleGallery
useEffect(() => {
  const stats = {
    stickerSetsCount: stickerSets.length,
    packsCount: galleryPacks.length,
    cacheStats: await cacheManager.getStats(),
    queueStats: await imageLoader.getQueueStats()
  };
  console.log('📊 Memory stats:', stats);
}, [stickerSets.length, galleryPacks.length]);
```

---

## Заключение

**Основная причина краша после 3 страниц**:
1. 🔴 **Накопление данных без ограничений** в `useStickerStore.stickerSets`
2. 🔴 **Отсутствие виртуализации** при начальной загрузке <50 элементов, что приводит к рендерингу всех элементов в DOM
3. 🟡 **Двойное кэширование** (Cache API + syncCache) усугубляет проблему

**Критичность**:
- После 3 страниц: ~70-80MB памяти
- После 5 страниц: ~200-250MB памяти
- На мобильных устройствах это может привести к крашу браузера/приложения

**Рекомендуемый порядок исправлений**:
1. Виртуализация (снизит нагрузку на DOM на 70-80%)
2. Ограничение размера массива (предотвратит неограниченный рост)
3. Оптимизация кэша и загрузки (улучшит общую производительность)















