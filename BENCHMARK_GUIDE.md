# 🎯 Руководство по Бенчмарк-Тестированию Галереи

## 📋 Содержание
- [Запуск тестов](#запуск-тестов)
- [Метрики производительности](#метрики-производительности)
- [Интерпретация результатов](#интерпретация-результатов)
- [Выявление узких мест](#выявление-узких-мест)
- [Рекомендации по оптимизации](#рекомендации-по-оптимизации)

---

## 🚀 Запуск тестов

### Базовый запуск
```bash
npm run test:e2e -- gallery-benchmark.spec.ts
```

### С визуализацией
```bash
npm run test:e2e:headed -- gallery-benchmark.spec.ts
```

### В режиме отладки
```bash
npm run test:e2e:debug -- gallery-benchmark.spec.ts
```

### Только мобильный тест
```bash
npm run test:e2e -- gallery-benchmark.spec.ts --grep @mobile
```

### Просмотр отчета
```bash
npm run test:e2e:report
```

---

## 📊 Метрики производительности

### 🕒 Время загрузки (Loading Timing)

#### **TTFS (Time To First Sticker)**
- **Описание**: Время от начала загрузки до появления первого стикера
- **Целевое значение**: < 3 секунды
- **Приемлемое значение**: < 5 секунд
- **Критическое значение**: > 5 секунд
- **Влияние**: Первое впечатление пользователя
- **Оптимизация**: 
  - Уменьшить размер bundle
  - Оптимизировать critical CSS
  - Использовать SSR/SSG
  - Prefetch первого стикера с максимальным приоритетом

#### **Time to First 6 Stickers**
- **Описание**: Время до загрузки первых 6 стикеров (первый экран на мобильном)
- **Целевое значение**: < 4 секунды
- **Приемлемое значение**: < 6 секунд
- **Критическое значение**: > 6 секунд
- **Влияние**: Восприятие скорости загрузки
- **Оптимизация**:
  - Приоритизация загрузки первых 6 карточек (TIER_1_FIRST_6_PACKS)
  - Использование IntersectionObserver для ленивой загрузки
  - Предзагрузка критических ресурсов

#### **Time to All 20 Stickers**
- **Описание**: Время до полной загрузки всех 20 стикеров первой страницы
- **Целевое значение**: < 8 секунд
- **Приемлемое значение**: < 12 секунд
- **Критическое значение**: > 15 секунд
- **Влияние**: Полнота контента на странице
- **Оптимизация**:
  - Прогрессивная загрузка (загружать по батчам)
  - Оптимизировать параллельность запросов
  - Использовать HTTP/2 multiplexing

#### **FCP (First Contentful Paint)**
- **Описание**: Время до первой отрисовки контента
- **Целевое значение**: < 1.8 секунды
- **Приемлемое значение**: < 3 секунды
- **Критическое значение**: > 3 секунды
- **Web Vitals**: Core Web Vital метрика
- **Оптимизация**:
  - Inline critical CSS
  - Минимизировать блокирующие ресурсы
  - Использовать resource hints (preload, preconnect)

#### **LCP (Largest Contentful Paint)**
- **Описание**: Время до отрисовки самого большого элемента контента
- **Целевое значение**: < 2.5 секунды
- **Приемлемое значение**: < 4 секунды
- **Критическое значение**: > 4 секунды
- **Web Vitals**: Core Web Vital метрика (очень важна для SEO)
- **Оптимизация**:
  - Оптимизировать изображения (размер, формат)
  - Использовать CDN
  - Preload LCP ресурса
  - Уменьшить TTFB (Time to First Byte)

#### **TTI (Time to Interactive)**
- **Описание**: Время до готовности страницы к взаимодействию
- **Целевое значение**: < 3.8 секунды
- **Приемлемое значение**: < 7.3 секунды
- **Критическое значение**: > 7.3 секунды
- **Влияние**: Интерактивность приложения
- **Оптимизация**:
  - Code splitting
  - Отложенная загрузка JavaScript
  - Уменьшить main thread work

---

### 🌐 Сетевые метрики (Network Metrics)

#### **Общее количество запросов**
- **Целевое значение**: < 50 запросов
- **Приемлемое значение**: < 100 запросов
- **Критическое значение**: > 150 запросов
- **Проблема**: Каждый запрос добавляет overhead (DNS, TCP, TLS)
- **Оптимизация**:
  - Объединение ресурсов (bundling)
  - Использование спрайтов
  - Lazy loading невидимого контента

#### **API запросы**
- **Целевое значение**: < 5 запросов
- **Приемлемое значение**: < 10 запросов
- **Проблема**: API запросы часто медленные (server processing)
- **Оптимизация**:
  - Batch API requests
  - GraphQL вместо REST (меньше запросов)
  - Агрессивное кеширование

#### **Дубликаты запросов**
- **Целевое значение**: 0 дубликатов
- **Приемлемое значение**: < 5 дубликатов
- **Критическое значение**: > 10 дубликатов
- **Проблема**: Трата bandwidth и времени на повторную загрузку
- **Оптимизация**:
  - Улучшить систему кеширования
  - Dedupe в imageLoader
  - Service Worker для кеша

#### **Неудачные запросы**
- **Целевое значение**: 0 ошибок
- **Критическое значение**: > 0 ошибок
- **Проблема**: Ошибки 4xx/5xx замедляют загрузку и ухудшают UX
- **Оптимизация**:
  - Fallback изображения
  - Retry логика с exponential backoff
  - Мониторинг и алерты

#### **Объем переданных данных**
- **Целевое значение**: < 5 MB
- **Приемлемое значение**: < 10 MB
- **Критическое значение**: > 15 MB
- **Проблема**: Медленно на мобильных сетях, траты трафика
- **Оптимизация**:
  - Сжатие изображений (WebP, AVIF)
  - Gzip/Brotli сжатие
  - Правильные размеры изображений (responsive images)
  - Lazy loading

#### **Среднее время ответа**
- **Целевое значение**: < 200ms
- **Приемлемое значение**: < 500ms
- **Критическое значение**: > 1000ms
- **Проблема**: Медленный сервер или сеть
- **Оптимизация**:
  - CDN для статики
  - Оптимизация backend
  - Database query optimization
  - Caching на backend

#### **Максимальная параллельность**
- **Целевое значение**: 6-12 параллельных запросов
- **Приемлемое значение**: < 30 запросов
- **Критическое значение**: > 50 запросов
- **Проблема**: Браузеры имеют лимит (обычно 6-8 на домен)
- **Проблема 2**: Слишком много параллельных запросов могут замедлить сеть
- **Оптимизация**:
  - Request queueing
  - Priority-based loading
  - Domain sharding (для HTTP/1.1)

---

### 🎨 Рендеринг и анимация (Rendering Metrics)

#### **FPS (Frames Per Second)**
- **Целевое значение**: 60 FPS
- **Приемлемое значение**: > 30 FPS
- **Критическое значение**: < 24 FPS
- **Проблема**: Низкий FPS = лаги и дерганная анимация
- **Оптимизация**:
  - CSS transforms вместо position
  - `will-change` для анимированных элементов
  - RequestAnimationFrame для JS анимаций
  - Virtualization для длинных списков
  - Throttle/debounce обработчиков скролла

#### **CLS (Cumulative Layout Shift)**
- **Целевое значение**: < 0.1
- **Приемлемое значение**: < 0.25
- **Критическое значение**: > 0.25
- **Web Vitals**: Core Web Vital метрика (важна для UX)
- **Проблема**: "Прыгающий" контент раздражает пользователей
- **Оптимизация**:
  - Указывать width/height для изображений
  - Резервировать место для динамического контента
  - Избегать вставки контента выше viewport
  - Использовать `aspect-ratio` в CSS
  - Skeleton loaders

#### **DOM узлов**
- **Целевое значение**: < 1500 элементов
- **Приемлемое значение**: < 3000 элементов
- **Критическое значение**: > 5000 элементов
- **Проблема**: Больше DOM = медленнее рендеринг и поиск элементов
- **Оптимизация**:
  - Виртуализация списков
  - Удаление невидимых элементов из DOM
  - Упрощение структуры компонентов

#### **Долгие задачи (Long Tasks)**
- **Целевое значение**: 0 задач > 50ms
- **Приемлемое значение**: < 5 задач > 50ms
- **Критическое значение**: > 10 задач > 50ms
- **Проблема**: Блокировка main thread = неотзывчивый UI
- **Оптимизация**:
  - Code splitting
  - Web Workers для тяжелых вычислений
  - Разбиение больших задач на мелкие части
  - `requestIdleCallback` для несрочной работы

---

### 💾 Использование ресурсов (Resource Usage)

#### **Память JS Heap**
- **Целевое значение**: < 50 MB
- **Приемлемое значение**: < 100 MB
- **Критическое значение**: > 150 MB
- **Проблема**: Высокое потребление памяти на мобильных устройствах
- **Оптимизация**:
  - Удалять неиспользуемые ссылки
  - Очистка кешей при достижении лимита
  - Object pooling для часто создаваемых объектов
  - Следить за утечками памяти

#### **Canvas контексты**
- **Целевое значение**: < 10 контекстов
- **Приемлемое значение**: < 20 контекстов
- **Критическое значение**: > 30 контекстов
- **Проблема**: Каждый canvas использует GPU память
- **Оптимизация**:
  - Переиспользование canvas
  - Удаление offscreen canvas
  - Virtualization для анимаций

---

### 📦 Кэширование (Caching Metrics)

#### **Эффективность кеша**
- **Целевое значение**: > 80%
- **Приемлемое значение**: > 50%
- **Критическое значение**: < 30%
- **Формула**: `cacheHits / (cacheHits + cacheMisses) * 100%`
- **Проблема**: Низкая эффективность = много лишних запросов
- **Оптимизация**:
  - Увеличить TTL кеша
  - Prefetch часто используемых ресурсов
  - Service Worker для offline кеширования
  - LRU/LFU политики вытеснения

---

## 🔍 Выявление узких мест

### Методология анализа

#### 1. **Waterfall Analysis** (Анализ водопада загрузки)
```
Что искать:
✅ Последовательные блокирующие загрузки (A → B → C)
   → Решение: Параллелизация, preload
   
✅ Длинные "пустые" промежутки между запросами
   → Решение: Prefetch, уменьшить latency
   
✅ Много мелких запросов подряд
   → Решение: Bundling, HTTP/2 Server Push
```

#### 2. **Request Timing Breakdown**
```
Типичные фазы запроса:
┌─────────────────────────────────────────┐
│ Queueing          │ 5-50ms    │ Ожидание в очереди браузера │
│ DNS Lookup        │ 20-120ms  │ Резолвинг домена             │
│ TCP Connection    │ 50-200ms  │ Установка TCP соединения     │
│ TLS Handshake     │ 100-500ms │ SSL/TLS рукопожатие          │
│ Request Sent      │ 1-10ms    │ Отправка запроса             │
│ Waiting (TTFB)    │ 50-500ms  │ Ожидание первого байта       │
│ Content Download  │ 10-1000ms │ Загрузка тела ответа         │
└─────────────────────────────────────────┘

Узкие места:
- DNS > 100ms → Используйте dns-prefetch
- TCP > 200ms → CDN ближе к пользователю
- TTFB > 500ms → Оптимизация backend/database
- Download > 1s → Сжатие, меньший размер файла
```

#### 3. **Rendering Pipeline Analysis**
```
Critical Rendering Path:
HTML → Parse → DOM
CSS  → Parse → CSSOM
       ↓
   Render Tree
       ↓
     Layout
       ↓
     Paint
       ↓
   Composite

Узкие места:
- Layout thrashing: читать → писать → читать DOM
- Forced synchronous layout: getBoundingClientRect() после изменения
- Paint storms: изменение свойств требующих repaint
```

#### 4. **Main Thread Analysis**
```
Что блокирует main thread:
1. JavaScript execution (скрипты)
2. Style calculations (CSS селекторы)
3. Layout (reflow)
4. Paint (перерисовка)
5. Composite (композитинг слоев)

Решения:
→ Defer non-critical JS
→ Упростить CSS селекторы
→ Batch DOM updates
→ Use CSS transforms (композитинг вместо layout)
```

---

## 🛠️ Рекомендации по оптимизации

### Приоритизация (Quick Wins)

#### 🔥 Критические (High Impact, Low Effort)
1. **Оптимизация изображений**
   - WebP вместо PNG/JPG (экономия 25-35%)
   - Правильные размеры (не загружать 2000px для 200px)
   - Lazy loading для offscreen контента

2. **Уменьшение дубликатов запросов**
   - Dedupe в imageLoader
   - Проверка кеша перед запросом

3. **HTTP кеширование**
   - Cache-Control headers (max-age)
   - ETag для валидации
   - Service Worker

4. **Приоритизация загрузки**
   - Tier-based loading (TIER_1 > TIER_2 > TIER_3)
   - Preload критических ресурсов
   - Defer несрочных скриптов

#### 🚀 Важные (High Impact, Medium Effort)
1. **Code splitting**
   - Разделение по роутам
   - Динамические импорты
   - Chunk optimization

2. **Virtualization**
   - Рендерить только видимые элементы
   - react-window или react-virtualized

3. **Prefetching & Preloading**
   - Prefetch следующей страницы
   - Preload критических ресурсов
   - Predictive prefetching (ML)

4. **CDN**
   - Статика на CDN
   - Image CDN с оптимизацией на лету
   - Edge caching

#### 💡 Полезные (Medium Impact, Medium Effort)
1. **Оптимизация анимаций**
   - CSS transforms вместо position
   - will-change для GPU acceleration
   - Throttle при скролле

2. **Database optimization**
   - Индексы для частых запросов
   - Query optimization
   - Connection pooling

3. **Monitoring & Analytics**
   - Real User Monitoring (RUM)
   - Synthetic monitoring
   - Performance budgets

---

## 📈 Continuous Performance Monitoring

### Автоматизация бенчмарков

#### В CI/CD pipeline
```yaml
# .github/workflows/performance.yml
name: Performance Benchmark

on:
  pull_request:
  schedule:
    - cron: '0 0 * * *' # Ежедневно

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run test:e2e -- gallery-benchmark.spec.ts
      - name: Compare with baseline
        run: node scripts/compare-performance.js
      - name: Comment PR
        uses: actions/github-script@v6
        with:
          script: |
            // Комментарий с результатами бенчмарка
```

#### Performance budgets
```json
{
  "budgets": {
    "ttfs": 3000,
    "lcp": 2500,
    "fcp": 1800,
    "cls": 0.1,
    "tbt": 300,
    "totalBytes": 5242880,
    "requests": 50
  }
}
```

#### Alerting
```javascript
// Отправка уведомлений при деградации
if (currentMetrics.lcp > baseline.lcp * 1.1) {
  sendSlackNotification(`⚠️ LCP увеличился на ${percentage}%`);
}
```

---

## 🔬 Advanced Techniques

### 1. **Progressive Hydration**
Гидратировать компоненты по мере прокрутки
```tsx
const PackCard = dynamic(() => import('./PackCard'), {
  loading: () => <SkeletonCard />,
  ssr: true
});
```

### 2. **Resource Hints**
```html
<link rel="dns-prefetch" href="//api.example.com">
<link rel="preconnect" href="//cdn.example.com">
<link rel="prefetch" href="/next-page.js">
<link rel="preload" href="/critical.css" as="style">
```

### 3. **Service Worker Strategies**
```javascript
// Stale-While-Revalidate для изображений
workbox.routing.registerRoute(
  /\.(?:png|jpg|webp)$/,
  new workbox.strategies.StaleWhileRevalidate({
    cacheName: 'images',
    plugins: [
      new workbox.expiration.Plugin({
        maxEntries: 100,
        maxAgeSeconds: 7 * 24 * 60 * 60, // 7 дней
      }),
    ],
  })
);
```

### 4. **Request Coalescing**
Объединение множества запросов в один
```typescript
class RequestBatcher {
  private queue: Request[] = [];
  private timeout: NodeJS.Timeout | null = null;
  
  add(request: Request) {
    this.queue.push(request);
    if (!this.timeout) {
      this.timeout = setTimeout(() => this.flush(), 50);
    }
  }
  
  flush() {
    const batch = this.queue.splice(0);
    fetch('/api/batch', { 
      method: 'POST', 
      body: JSON.stringify(batch) 
    });
    this.timeout = null;
  }
}
```

---

## 📚 Дополнительные ресурсы

### Инструменты
- **Chrome DevTools**: Performance panel, Network panel, Lighthouse
- **WebPageTest**: Детальный анализ waterfall
- **Lighthouse CI**: Автоматизация Lighthouse в CI/CD
- **SpeedCurve**: Continuous performance monitoring
- **Bundle Analyzer**: webpack-bundle-analyzer

### Чек-листы
- [Web.dev Performance Checklist](https://web.dev/performance/)
- [Front-End Performance Checklist 2024](https://www.smashingmagazine.com/2021/01/front-end-performance-2021-free-pdf-checklist/)

### Метрики и стандарты
- [Core Web Vitals](https://web.dev/vitals/)
- [User-centric Performance Metrics](https://web.dev/user-centric-performance-metrics/)

---

## 💼 Заключение

**Ключевые принципы оптимизации:**

1. 🎯 **Измеряйте**: Невозможно оптимизировать то, что не измеряете
2. 📊 **Базовая линия**: Создайте baseline метрики для сравнения
3. 🔄 **Итеративность**: Оптимизируйте постепенно, проверяя изменения
4. 👥 **Пользователь в центре**: Фокус на User Experience, не только на технических метриках
5. 🤖 **Автоматизация**: CI/CD integration для регрессионного тестирования
6. 📈 **Мониторинг**: Real User Monitoring для production

**Помните:** Преждевременная оптимизация - корень всех зол. Оптимизируйте там, где есть реальные проблемы, подтвержденные метриками.

---

*Последнее обновление: Ноябрь 2024*

