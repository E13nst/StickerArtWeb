# 📋 Полное техническое описание проекта StickerArtWeb

**Дата анализа:** 2025-01-27  
**Версия проекта:** 1.0.0

---

## 1. Архитектура

### 1.1 Общая архитектура

Проект представляет собой **Telegram Mini App** для галереи стикеров с разделением на фронтенд и бэкенд:

```
┌─────────────────────────────────────────┐
│  Frontend (React + Vite)                │
│  ┌─────────────────────────────────┐    │
│  │  Nginx Container (Amvera)       │    │
│  │  ├── Static files (dist/)       │    │
│  │  └── Proxy /api → Backend       │────┼───┐
│  └─────────────────────────────────┘    │   │
└─────────────────────────────────────────┘   │
                                              │
┌─────────────────────────────────────────┐   │
│  Backend (Spring Boot)                   │◄──┘
│  ┌─────────────────────────────────┐    │
│  │  API endpoints (/api)           │    │
│  │  ├── Auth (/auth)               │    │
│  │  ├── Stickersets                │    │
│  │  ├── Likes                      │    │
│  │  ├── Profiles                   │    │
│  │  └── Categories                 │    │
│  │  └── Database (PostgreSQL)      │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

### 1.2 Фронтенд

**Технологический стек:**
- **React 18.2.0** — UI библиотека
- **TypeScript 5.2.2** — типизация
- **Vite 5.0.0** — сборщик и dev-сервер
- **Zustand 4.4.7** — управление состоянием
- **React Router DOM 6.20.1** — маршрутизация
- **Material-UI 5.15.0** — UI компоненты
- **Axios 1.6.2** — HTTP клиент
- **@twa-dev/sdk 8.0.2** — Telegram Web App SDK
- **Framer Motion 11.18.2** — анимации
- **Lottie React 2.4.0** — анимации (Lottie)

**Структура директорий:**

```
miniapp/src/
├── api/                    # API клиент и конфигурация
│   ├── client.ts          # Основной API клиент (Axios)
│   └── config.ts          # Конфигурация приложения
├── components/            # React компоненты (40+ файлов)
│   ├── GalleryGrid.tsx    # Сетка галереи
│   ├── PackCard.tsx       # Карточка стикерпака
│   ├── StickerPackModal.tsx # Модалка деталей
│   ├── CategoryFilter.tsx # Фильтр категорий
│   ├── SearchBar.tsx      # Поиск
│   ├── BottomNav.tsx      # Нижняя навигация
│   └── ...                # Множество других компонентов
├── hooks/                 # React хуки
│   ├── useTelegram.ts    # Интеграция с Telegram Web App
│   ├── useAuth.ts        # Авторизация
│   ├── useDebounce.ts    # Debounce для поиска
│   └── ...
├── pages/                # Страницы приложения
│   ├── DashboardPage.tsx # Главная (статистика)
│   ├── GalleryPage.tsx   # Галерея стикеров
│   ├── MyProfilePage.tsx # Мой профиль
│   └── ProfilePage.tsx  # Профиль другого пользователя
├── store/                # Zustand stores
│   ├── useStickerStore.ts    # Состояние стикерсетов
│   ├── useLikesStore.ts      # Состояние лайков (с persist)
│   ├── useProfileStore.ts    # Состояние профилей (с кэшем)
│   └── useGalleryStore.ts    # Состояние галереи (пустой)
├── types/                # TypeScript типы
│   ├── sticker.ts        # Типы стикеров и стикерсетов
│   └── telegram.ts       # Типы Telegram API
├── utils/                # Утилиты
│   ├── galleryAdapter.ts # Адаптеры для галереи
│   ├── imageLoader.ts    # Загрузка изображений
│   ├── stickerUtils.ts   # Утилиты для стикеров
│   └── ...
├── layouts/              # Layout компоненты
│   └── MainLayout.tsx   # Основной layout
├── data/                # Моковые данные (для dev)
└── App.tsx              # Корневой компонент
```

**Маршрутизация:**

```typescript
Routes:
  / (или /dashboard) → DashboardPage
  /gallery          → GalleryPage
  /profile          → MyProfilePage
  /profile/:userId  → ProfilePage
  /*                → DashboardPage (fallback)
```

**Base path:** `/miniapp/` (настроено в `vite.config.ts`)

### 1.3 Бэкенд (внешний)

**Технологии (из документации):**
- **Spring Boot** — Java фреймворк
- **PostgreSQL** — база данных
- **REST API** — архитектура

**URL бэкенда:** `https://stickerartgallery-e13nst.amvera.io`

**Проксирование:**
- Nginx проксирует `/api/*` → `${BACKEND_URL}/api/*`
- Все запросы идут через фронтенд Nginx

### 1.4 Деплой

**Платформа:** Amvera (Node.js + Nginx)

**Процесс деплоя:**
1. Git push → GitHub webhook
2. Amvera автоматически:
   - `npm ci` — установка зависимостей
   - `npm run build` — сборка в `dist/miniapp/`
   - Docker build (multi-stage: Node.js → Nginx)
   - Docker run (Nginx раздает статику)

**Переменные окружения:**
- `BACKEND_URL` — URL бэкенд API
- `VITE_BACKEND_URL` — для dev режима

**Хранение данных:**
- `/data/cache/nginx/stickers` — persistent кэш стикеров (14 дней, до 5GB)
- Кэш сохраняется между деплоями

---

## 2. Функциональность

### 2.1 Основные функции

#### ✅ Реализовано:

1. **Галерея стикеров**
   - Просмотр стикерсетов с пагинацией
   - Infinite scroll (виртуализация)
   - Поиск по названию
   - Фильтрация по категориям (множественный выбор)
   - Сортировка (по дате, лайкам)
   - Модальное окно с деталями стикерсета

2. **Лайки**
   - Лайк/дизлайк стикерсетов
   - Оптимистичные обновления UI
   - Синхронизация с сервером (debounce 500ms)
   - Offline поддержка (очередь синхронизации)
   - Rate limiting (1 сек между запросами)
   - Persist в localStorage (версия 3)

3. **Профили пользователей**
   - Просмотр своего профиля
   - Просмотр профилей других пользователей
   - Список стикерсетов пользователя
   - Фильтр "Опубликованные" / "Понравившиеся"
   - Кэширование профилей (TTL 5 минут, in-memory)
   - Баланс ART токенов
   - Роль пользователя (USER, ADMIN, etc.)

4. **Dashboard (главная страница)**
   - Статистика: всего стикерсетов, лайков, ART
   - Топ-5 категорий по лайкам
   - Топ-5 авторов по количеству стикеров
   - Топ-5 стикерсетов по лайкам
   - Тренды (рост за день/сегодня)

5. **Загрузка стикерпаков**
   - Загрузка по ссылке Telegram (`https://t.me/addstickers/...`)
   - Модальное окно загрузки
   - Валидация ссылки

6. **Категории**
   - 17 категорий (animals, memes, emotions, cute, anime, etc.)
   - Локализация (ru/en через заголовок `X-Language`)
   - Фильтрация стикерсетов по категориям
   - Множественный выбор категорий

7. **Telegram интеграция**
   - Авторизация через Telegram Web App
   - Использование Telegram темы (dark/light)
   - Haptic feedback
   - Адаптация под Telegram UI
   - Mock режим для разработки вне Telegram

8. **Оптимизации**
   - Lazy loading изображений
   - Виртуализация списков
   - Кэширование API запросов (Nginx, 10 минут)
   - Кэширование стикеров (Nginx, 14 дней)
   - Debounce для поиска (500ms)
   - Оптимистичные обновления UI

#### 🚧 В разработке / частично:

1. **ART токены**
   - Баланс отображается в профиле
   - API endpoint `/api/profiles/me` возвращает `artBalance`
   - Логика начисления/трат не видна во фронтенде (только отображение)

2. **Экспорт стикеров**
   - Не реализовано во фронтенде
   - Возможно есть на бэкенде

3. **Коллекции**
   - Компонент `PersonalCollections.tsx` существует, но не интегрирован
   - Тип `Collection` определен, но не используется

### 2.2 API эндпоинты

#### Авторизация:
- `GET /api/auth/status` — проверка статуса авторизации
- Авторизация через заголовок `X-Telegram-Init-Data`

#### Стикерсеты:
- `GET /api/stickersets` — список стикерсетов (пагинация)
  - Параметры: `page`, `size`, `categoryKeys`, `likedOnly`, `sort`, `direction`
- `GET /api/stickersets/{id}` — детали стикерсета
- `GET /api/stickersets/search` — поиск по названию
- `GET /api/stickersets/user/{userId}` — стикерсеты пользователя
- `GET /api/stickersets/user/{userId}/search` — поиск стикерсетов пользователя
- `POST /api/stickersets/import` — загрузка стикерпака по ссылке
- `DELETE /api/stickersets/{id}` — удаление стикерсета

#### Лайки:
- `PUT /api/likes/stickersets/{stickerSetId}/toggle` — переключить лайк
- `GET /api/likes/stickersets` — лайкнутые стикерсеты (пагинация)
- `GET /api/likes/top-stickersets` — топ стикерсетов по лайкам

#### Профили:
- `GET /api/profiles/me` — профиль текущего пользователя
- `GET /api/profiles/{userId}` — профиль пользователя
- `GET /api/users/{userId}/photo` — фото профиля

#### Категории:
- `GET /api/categories` — список категорий
  - Поддержка локализации через `X-Language` (ru/en)

#### Прокси стикеров:
- `GET /stickers/{fileId}` — прямой прокси через Nginx в sticker-processor (кэш 14 дней, до 5GB; поддерживает `?file=true`)

#### Конфигурация:
- `GET /api/config` — конфигурация приложения (botName, miniAppUrl)

### 2.3 Пользовательские сценарии

#### Сценарий 1: Просмотр галереи
1. Пользователь открывает приложение → Dashboard
2. Переходит в "Галерея" (BottomNav)
3. Видит список стикерсетов с пагинацией
4. Может фильтровать по категориям
5. Может искать по названию
6. Кликает на стикерсет → открывается модалка с деталями
7. Может лайкнуть/дизлайкнуть

#### Сценарий 2: Лайк стикерсета
1. Пользователь кликает на лайк
2. UI обновляется мгновенно (оптимистично)
3. Через 500ms отправляется запрос на сервер
4. При успехе — синхронизация с сервером
5. При ошибке — откат изменений + добавление в очередь

#### Сценарий 3: Загрузка стикерпака
1. Пользователь открывает свой профиль
2. Нажимает "Добавить стикерпак"
3. Вводит ссылку Telegram
4. Отправляется запрос `POST /api/stickersets/import`
5. Стикерсет добавляется в профиль

#### Сценарий 4: Просмотр профиля
1. Пользователь открывает профиль (свой или чужой)
2. Видит информацию о пользователе (имя, баланс ART, роль)
3. Видит список стикерсетов пользователя
4. Может переключать фильтр "Опубликованные" / "Понравившиеся"
5. Может сортировать по дате/лайкам

---

## 3. Данные и интеграции

### 3.1 Модели данных (фронтенд)

#### StickerSetResponse:
```typescript
{
  id: number;
  name: string;                    // Уникальное имя (Telegram)
  title: string;                    // Название
  createdAt: string;                // ISO дата
  updatedAt: string;                // ISO дата
  telegramStickerSetInfo: {         // Информация из Telegram
    name: string;
    title: string;
    is_animated: boolean;
    is_video: boolean;
    contains_masks: boolean;
    stickers: Sticker[];            // Массив стикеров
    thumbnail?: {...}
  };
  userId?: number;                  // ID автора
  username?: string;
  firstName?: string;
  lastName?: string;
  avatarUrl?: string;
  likesCount?: number;              // Общее количество лайков
  isLikedByCurrentUser?: boolean;   // Лайкнул ли текущий пользователь
  categories?: Array<{              // Категории
    id: number;
    key: string;                    // animals, memes, etc.
    name: string;                   // Локализованное название
    description: string;
    displayOrder: number;
    isActive: boolean;
  }>;
}
```

#### UserInfo:
```typescript
{
  id: number;
  telegramId: number;
  username?: string;
  firstName?: string;
  lastName?: string;
  avatarUrl?: string;
  role: string;                     // USER, ADMIN, etc.
  artBalance: number;              // Баланс ART токенов
  createdAt: string;
  updatedAt?: string;
  profilePhotoFileId?: string;     // file_id для загрузки фото
  telegramUserInfo?: {...}         // Доп. данные из Telegram
}
```

#### CategoryResponse:
```typescript
{
  id: number;
  key: string;                     // animals, memes, etc.
  name: string;                     // Локализованное название
  description: string;
  iconUrl?: string;
  displayOrder: number;
  isActive: boolean;
}
```

### 3.2 База данных (бэкенд)

**Предполагаемая структура (из API):**

**Таблицы:**
- `users` — пользователи (Telegram ID, username, имя, роль)
- `profiles` — профили (userId, role, artBalance)
- `sticker_sets` — стикерсеты (id, name, title, userId, createdAt)
- `stickers` — стикеры (file_id, file_unique_id, set_id, type, etc.)
- `categories` — категории (id, key, name, description, displayOrder, isActive)
- `sticker_set_categories` — связь стикерсетов и категорий (many-to-many)
- `likes` — лайки (user_id, sticker_set_id, created_at)

**Связи:**
- User → Profile (1:1)
- User → StickerSet (1:many)
- StickerSet → Sticker (1:many)
- StickerSet ↔ Category (many:many)
- User → Like → StickerSet (many:many через likes)

### 3.3 Хранение файлов

**Стикеры:**
- Хранятся в Telegram (через `file_id`)
- Проксируются через Nginx: `/stickers/{fileId}` (прямой прокси в sticker-processor)
- Кэшируются в Nginx: `/data/cache/nginx/stickers` (14 дней, до 5GB)

**S3/внешнее хранилище:**
- Не видно во фронтенде
- Возможно используется на бэкенде для кэширования

### 3.4 Платежи / ART токены

**ART токены:**
- Баланс отображается в профиле
- API возвращает `artBalance` в `/api/profiles/me`
- Логика начисления/трат не видна во фронтенде

**Платежи:**
- Не реализовано во фронтенде
- Возможно используется Telegram Payments на бэкенде

---

## 4. UX и UI

### 4.1 Основные экраны

#### 1. Dashboard (главная)
- **Путь:** `/` или `/dashboard`
- **Компонент:** `DashboardPage.tsx`
- **Содержимое:**
  - Статистика (наборы, лайки, ART)
  - Топ-5 авторов
  - Топ-5 категорий
  - Топ-5 стикерсетов (горизонтальный скролл)

#### 2. Галерея
- **Путь:** `/gallery`
- **Компонент:** `GalleryPage.tsx`
- **Содержимое:**
  - Панель категорий (горизонтальный скролл)
  - Поисковая строка
  - Сетка стикерсетов (infinite scroll)
  - Кнопка "Добавить стикерпак"
  - Модальное окно деталей стикерсета

#### 3. Мой профиль
- **Путь:** `/profile`
- **Компонент:** `MyProfilePage.tsx`
- **Содержимое:**
  - Информация о пользователе (имя, баланс ART, роль)
  - Табы: "Сеты", "Баланс", "Поделиться"
  - Фильтр "Опубликованные" / "Понравившиеся"
  - Список стикерсетов
  - Кнопка "Добавить стикерпак"

#### 4. Профиль другого пользователя
- **Путь:** `/profile/:userId`
- **Компонент:** `ProfilePage.tsx`
- **Содержимое:**
  - Информация о пользователе
  - Список стикерсетов пользователя
  - Нет доступа к балансу ART

### 4.2 Навигация

**BottomNav (нижняя панель):**
- Dashboard (индекс 0)
- Галерея (индекс 1)
- Поиск (индекс 2) — не реализовано
- Профиль (индекс 3)

**Верхний header:**
- `StixlyTopHeader` — карусель с логотипом (на всех страницах кроме профиля)

### 4.3 Основные компоненты UI

#### PackCard
- Карточка стикерсета
- Превью (thumbnail)
- Название
- Кнопка лайка
- Счетчик лайков

#### StickerPackModal
- Модальное окно с деталями стикерсета
- Превью всех стикеров
- Информация об авторе
- Кнопка лайка
- Кнопка "Добавить в Telegram"

#### CategoryFilter
- Горизонтальная прокручиваемая панель
- Множественный выбор категорий
- Haptic feedback

#### SearchBar
- Поисковая строка с debounce
- Автодополнение (не реализовано)

#### SimpleGallery
- Виртуализированная сетка стикерсетов
- Infinite scroll
- Lazy loading изображений

### 4.4 Пользовательские потоки

#### Поток 1: Просмотр и лайк стикерсета
```
Dashboard → Галерея → Фильтр категорий → Выбор стикерсета → 
Модалка деталей → Лайк → Синхронизация с сервером
```

#### Поток 2: Загрузка стикерпака
```
Профиль → "Добавить стикерпак" → Ввод ссылки → 
Загрузка → Обновление списка
```

#### Поток 3: Просмотр профиля
```
Галерея → Клик на автора → Профиль автора → 
Просмотр стикерсетов → Лайк стикерсета
```

---

## 5. Текущее состояние

### 5.1 ✅ Готово и работает

1. **Базовая функциональность:**
   - ✅ Просмотр галереи стикеров
   - ✅ Лайки с синхронизацией
   - ✅ Профили пользователей
   - ✅ Поиск и фильтрация
   - ✅ Загрузка стикерпаков
   - ✅ Dashboard со статистикой

2. **Оптимизации:**
   - ✅ Кэширование (Nginx, localStorage)
   - ✅ Виртуализация списков
   - ✅ Lazy loading изображений
   - ✅ Debounce для поиска
   - ✅ Оптимистичные обновления UI

3. **Telegram интеграция:**
   - ✅ Авторизация через Telegram Web App
   - ✅ Адаптация под Telegram тему
   - ✅ Haptic feedback
   - ✅ Mock режим для разработки

4. **UI/UX:**
   - ✅ Адаптивный дизайн
   - ✅ Темная/светлая тема
   - ✅ Анимации (Framer Motion, Lottie)
   - ✅ Навигация (BottomNav)

### 5.2 🚧 В разработке / частично

1. **ART токены:**
   - ✅ Отображение баланса
   - ❌ Логика начисления/трат (не видна во фронтенде)
   - ❌ Покупки/продажи стикеров

2. **Коллекции:**
   - ✅ Компонент `PersonalCollections.tsx` существует
   - ❌ Не интегрирован в приложение
   - ❌ API эндпоинты не видны

3. **Экспорт стикеров:**
   - ❌ Не реализовано во фронтенде
   - ❓ Возможно есть на бэкенде

4. **Поиск с автодополнением:**
   - ✅ Базовая строка поиска
   - ❌ Автодополнение не реализовано
   - ✅ Компонент `SearchWithSuggestions.tsx` существует, но не используется

### 5.3 ❌ Не реализовано / требует доработки

1. **Платежи:**
   - ❌ Интеграция с Telegram Payments
   - ❌ Покупка премиум стикеров
   - ❌ Пополнение баланса ART

2. **Уведомления:**
   - ❌ Push уведомления
   - ❌ Уведомления о новых стикерах

3. **Социальные функции:**
   - ❌ Комментарии к стикерсетам
   - ❌ Поделиться стикерсетом
   - ❌ Подписки на авторов

4. **Админ панель:**
   - ❌ Управление категориями
   - ❌ Модерация стикерсетов
   - ❌ Статистика платформы

5. **Экспорт/импорт:**
   - ❌ Экспорт стикерсета в ZIP
   - ❌ Импорт из файла

6. **Тестирование:**
   - ✅ Playwright тесты настроены
   - ❌ E2E тесты не написаны (только конфигурация)
   - ❌ Unit тесты отсутствуют

---

## 6. Вопросы для уточнения

### 6.1 Архитектура

1. **Бэкенд:**
   - Где находится код бэкенда? (отдельный репозиторий?)
   - Какие технологии используются? (Spring Boot версия, Java версия)
   - Есть ли документация API? (Swagger доступен?)

2. **База данных:**
   - Какая версия PostgreSQL?
   - Есть ли миграции? (Flyway, Liquibase?)
   - Какие индексы созданы?

3. **Хранилище файлов:**
   - Используется ли S3 или другое облачное хранилище?
   - Как организовано кэширование стикеров на бэкенде?

### 6.2 Функциональность

4. **ART токены:**
   - Как начисляются ART токены?
   - Как тратятся ART токены?
   - Есть ли история транзакций?

5. **Платежи:**
   - Планируется ли интеграция с Telegram Payments?
   - Как работает покупка премиум стикеров?

6. **Коллекции:**
   - Что такое "коллекции"? (пользовательские папки?)
   - Планируется ли интеграция компонента `PersonalCollections`?

7. **Экспорт:**
   - Есть ли API для экспорта стикерсета в ZIP?
   - Нужно ли это реализовывать?

### 6.3 UX/UI

8. **Поиск:**
   - Нужно ли автодополнение в поиске?
   - Есть ли API для suggestions?

9. **Уведомления:**
   - Планируются ли push уведомления?
   - Какие события должны триггерить уведомления?

10. **Социальные функции:**
    - Нужны ли комментарии к стикерсетам?
    - Нужны ли подписки на авторов?

### 6.4 Техническое

11. **Производительность:**
    - Какие метрики производительности важны?
    - Есть ли проблемы с производительностью?

12. **Мониторинг:**
    - Используется ли мониторинг? (Sentry, LogRocket?)
    - Есть ли аналитика? (Google Analytics, Yandex Metrika?)

13. **Безопасность:**
    - Как валидируется `X-Telegram-Init-Data` на бэкенде?
    - Есть ли rate limiting на бэкенде?

14. **Тестирование:**
    - Нужно ли писать E2E тесты?
    - Нужны ли unit тесты?

---

## 7. Резюме

### Сильные стороны:
- ✅ Современный стек технологий (React, TypeScript, Vite)
- ✅ Хорошая архитектура (разделение на слои, Zustand stores)
- ✅ Оптимизации (кэширование, виртуализация, lazy loading)
- ✅ Telegram интеграция (авторизация, темы, haptic feedback)
- ✅ Адаптивный UI с поддержкой темной/светлой темы

### Области для улучшения:
- 🚧 ART токены (логика начисления/трат)
- 🚧 Коллекции (интеграция компонента)
- ❌ Платежи (Telegram Payments)
- ❌ Социальные функции (комментарии, подписки)
- ❌ Тестирование (E2E, unit тесты)

### Приоритеты развития:
1. **Высокий:** Завершить интеграцию ART токенов, платежи
2. **Средний:** Коллекции, экспорт стикеров, уведомления
3. **Низкий:** Социальные функции, админ панель

---

**Документ создан:** 2025-01-27  
**Версия:** 1.0.0






