# Паттерны фона страниц

SVG-паттерны для фона (Dashboard, MyProfile и др.): `heart-fill.svg`, `heart-stroke.svg`.

- Используются компонентом `PatternBackground` (проп `patternSrc`).
- Для масштабирования (выбор эмодзи с API) подставлять URL в `patternSrc`.
