# 🚀 Скрипты для управления dev-сервером

## Быстрый старт

```bash
# Из корневой директории проекта
npm start    # Запустить dev-сервер
npm stop     # Остановить dev-сервер
npm restart  # Перезапустить dev-сервер
```

## Доступные скрипты

### 1. `simple-start.ps1` - Запуск dev-сервера

**Использование:**
```powershell
.\scripts\simple-start.ps1
# или
npm start
```

**Что делает:**
- Переходит в директорию `miniapp/`
- Проверяет наличие `node_modules`, устанавливает зависимости при необходимости
- Останавливает процесс на порту 3000, если он уже запущен
- Устанавливает переменную окружения `VITE_BACKEND_URL`
- Запускает Vite dev-сервер на порту **3000** с доступом из сети (`--host`)

**Параметры запуска:**
- Port: `3000`
- Host: `0.0.0.0` (доступен из локальной сети)
- Backend URL: `https://stickerartgallery-e13nst.amvera.io`

**Доступ к приложению:**
- Local: `http://localhost:3000/miniapp/`
- Network: `http://[your-IP]:3000/miniapp/`

---

### 2. `simple-stop.ps1` - Остановка dev-сервера

**Использование:**
```powershell
.\scripts\simple-stop.ps1
# или
npm stop
```

**Что делает:**
- Находит процесс, слушающий порт 3000
- Останавливает этот процесс
- Опционально: может остановить все Node.js процессы (по умолчанию отключено)

**Безопасность:**
По умолчанию останавливает **только** процесс на порту 3000. Если нужно остановить все Node процессы, измените в скрипте:
```powershell
$stopAllNode = $true  # Осторожно! Остановит все Node процессы
```

---

### 3. `simple-restart.ps1` - Перезапуск dev-сервера

**Использование:**
```powershell
.\scripts\simple-restart.ps1
# или
npm restart
```

**Что делает:**
1. Вызывает `simple-stop.ps1` для остановки сервера
2. Ждет 2 секунды для очистки ресурсов
3. Вызывает `simple-start.ps1` для запуска сервера

**Когда использовать:**
- После изменения конфигурации Vite
- После обновления зависимостей
- При проблемах с горячей перезагрузкой (HMR)
- После изменения переменных окружения

---

### 4. `start-dev.ps1` - Расширенный запуск с проверками

**Использование:**
```powershell
.\scripts\start-dev.ps1
```

**Что делает (дополнительно к `simple-start.ps1`):**
- Проверяет TypeScript на ошибки (не блокирует запуск)
- Запускает сервер в фоновом PowerShell Job
- Проверяет доступность HTTP endpoint
- Показывает расширенную информацию о статусе
- Ожидает завершения или Ctrl+C для выхода

**Когда использовать:**
- Для подробной диагностики при проблемах
- Когда нужен контроль над процессом запуска
- Для CI/CD или автоматизированных тестов

---

## Переменные окружения

Все скрипты автоматически устанавливают:

```powershell
$env:VITE_BACKEND_URL = "https://stickerartgallery-e13nst.amvera.io"
```

Для изменения backend URL отредактируйте скрипты или установите переменную вручную:

```powershell
$env:VITE_BACKEND_URL = "http://localhost:8080"
.\scripts\simple-start.ps1
```

---

## Устранение неполадок

### Проблема: "Порт 3000 уже занят"

**Решение 1:** Остановить существующий процесс
```powershell
npm stop
```

**Решение 2:** Вручную найти и остановить процесс
```powershell
# Найти процесс на порту 3000
Get-NetTCPConnection -LocalPort 3000 | Select-Object OwningProcess

# Остановить процесс (замените PID на реальный ID)
Stop-Process -Id <PID> -Force
```

**Решение 3:** Использовать другой порт
```powershell
# В miniapp/vite.config.ts измените port в server.port
# Или запустите напрямую:
cd miniapp
npm run dev -- --port 3001
```

---

### Проблема: "Сервер не запускается"

**Проверка 1:** Установлены ли зависимости
```powershell
cd miniapp
npm install
```

**Проверка 2:** TypeScript ошибки
```powershell
cd miniapp
npx tsc --noEmit
```

**Проверка 3:** Проверка Node.js версии
```powershell
node --version  # Требуется >= 16.x
npm --version
```

---

### Проблема: "Cannot find module"

**Решение:** Переустановить зависимости
```powershell
cd miniapp
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm install
```

---

### Проблема: "Server responds with 404"

**Проверка 1:** Убедитесь, что используете правильный URL
```
✓ http://localhost:3000/miniapp/
✗ http://localhost:3000/
```

**Проверка 2:** Проверьте vite.config.ts
```typescript
export default defineConfig({
  base: '/miniapp/',  // Должно быть установлено
  // ...
})
```

---

## Полезные команды

### Проверка статуса сервера
```powershell
# Проверить порт 3000
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue

# Проверить все Node процессы
Get-Process node

# HTTP проверка
Invoke-WebRequest -Uri "http://localhost:3000/miniapp/" -UseBasicParsing
```

### Очистка и перезапуск
```powershell
# Полная очистка
npm stop
cd miniapp
Remove-Item -Recurse -Force node_modules, dist, .vite
npm install
cd ..
npm start
```

### Просмотр логов
```powershell
# Если использовался start-dev.ps1 в фоне
Get-Job | Receive-Job
```

---

## Архитектура

```
StickerArtWeb-1/
├── scripts/
│   ├── simple-start.ps1      # Основной скрипт запуска
│   ├── simple-stop.ps1       # Остановка сервера
│   ├── simple-restart.ps1    # Перезапуск (stop + start)
│   ├── start-dev.ps1         # Расширенный запуск
│   └── ...
├── miniapp/                  # Приложение
│   ├── src/
│   ├── vite.config.ts
│   └── package.json
└── package.json              # npm scripts здесь

```

## Команды NPM

Все скрипты доступны через NPM из корневой директории:

| Команда | Скрипт | Описание |
|---------|--------|----------|
| `npm start` | `simple-start.ps1` | Быстрый запуск сервера |
| `npm stop` | `simple-stop.ps1` | Остановка сервера |
| `npm restart` | `simple-restart.ps1` | Перезапуск сервера |
| `npm run rebuild` | `simple-rebuild.ps1` | Пересборка приложения |
| `npm run check` | `simple-check.ps1` | Проверка приложения |

---

## Требования

- **Node.js**: >= 16.x
- **npm**: >= 7.x
- **PowerShell**: >= 5.1 (Windows) или PowerShell Core 7+ (Cross-platform)
- **Права**: Execution Policy должен разрешать запуск скриптов

### Настройка Execution Policy (если требуется)

```powershell
# Только для текущего пользователя (рекомендуется)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Или запуск с обходом политики (используется в package.json)
powershell -ExecutionPolicy Bypass -File scripts/simple-start.ps1
```

---

## Разработка

При разработке новых скриптов следуйте правилам:

1. ✅ Всегда переходите в `miniapp/` директорию
2. ✅ Устанавливайте `VITE_BACKEND_URL`
3. ✅ Используйте порт **3000** (как в rules.mdc)
4. ✅ Добавляйте `--host` для доступа из сети
5. ✅ Проверяйте существующие процессы перед запуском
6. ✅ Добавляйте информативные сообщения с цветом
7. ✅ Обрабатывайте ошибки gracefully

---

## Связанные файлы

- `.cursor/rules/rules.mdc` - Правила запуска dev-сервера
- `miniapp/vite.config.ts` - Конфигурация Vite
- `package.json` - NPM скрипты
- `scripts/README.md` - Общая документация по скриптам

---

**Последнее обновление:** 2025-10-31


















