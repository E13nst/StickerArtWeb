# Backend API URL
# Для dev: используется в vite.config.ts для proxy
# Для prod: передается в nginx через BACKEND_URL
VITE_BACKEND_URL=http://localhost:8080

# Production backend URL (для Nginx в Docker)
# Для Docker/Nginx (прод) можно переопределить на прод-URL
BACKEND_URL=https://stickerartgallery-e13nst.amvera.io

# Sticker processor direct path (served via Nginx location /stickers)
VITE_STICKER_PROCESSOR_PATH=/stickers

# Optional: override for local development proxy target (host origin without /stickers suffix)
# VITE_STICKER_PROCESSOR_PROXY_TARGET=https://sticker-processor-e13nst.amvera.io

# Optional: use when bypassing Nginx entirely (absolute URL including /stickers suffix)
# VITE_STICKER_PROCESSOR_URL=https://sticker-processor-e13nst.amvera.io/stickers

