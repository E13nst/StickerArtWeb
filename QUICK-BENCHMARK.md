# ⚡ Быстрый старт бенчмарк-теста

## 🚀 Один запуск - всё узнаешь

```bash
# 1. Запустить бенчмарк
npm run test:e2e -- gallery-benchmark.spec.ts

# 2. Посмотреть результаты
npm run test:e2e:report
```

## 📊 Что измеряется

### ⏱️ Время загрузки
- Первый стикер (цель: <3s)
- Первые 6 стикеров (цель: <4s)
- Все 20 стикеров (цель: <8s)

### 🌐 Сеть
- Количество запросов (цель: <50)
- Дубликаты (цель: 0)
- Объем данных (цель: <5MB)

### 🎨 Производительность
- FPS (цель: 60)
- Layout Shifts (цель: <0.1)
- Память (цель: <100MB)

## 🎯 Оценки

| Оценка | Скор | Что значит |
|--------|------|------------|
| 🏆 **A** | 90-100 | Отлично! |
| ✅ **B** | 80-89 | Хорошо |
| ⚠️ **C** | 70-79 | Есть что улучшить |
| ❌ **D** | 60-69 | Плохо |
| 💀 **F** | <60 | Критично |

## 🔧 Быстрые фиксы

### Медленная загрузка?
```typescript
// Preload критических ресурсов
<link rel="preload" href="/first-sticker.webp" as="image">
```

### Много дубликатов?
```typescript
// Проверь imageLoader - есть ли dedupe?
if (pendingRequests.has(url)) {
  return pendingRequests.get(url);
}
```

### Низкий FPS?
```css
/* Используй CSS transforms */
.sticker {
  transform: translate3d(0, 0, 0);
  will-change: transform;
}
```

### Layout Shifts?
```tsx
// Указывай размеры
<img width={200} height={300} src={url} />
```

## 📈 Варианты запуска

```bash
# Базовый
npm run test:e2e -- gallery-benchmark.spec.ts

# С браузером (видеть что происходит)
npm run test:e2e:headed -- gallery-benchmark.spec.ts

# Только мобильный
npm run test:e2e -- gallery-benchmark.spec.ts --grep @mobile

# Дебаг
npm run test:e2e:debug -- gallery-benchmark.spec.ts
```

## 💡 Частые проблемы

| Проблема | Что делать |
|----------|------------|
| TTFS > 5s | Preload, code splitting, SSR |
| Дубликаты > 10 | Улучши кеш и dedupe |
| FPS < 30 | CSS transforms, virtualization |
| CLS > 0.25 | Укажи размеры, skeleton loaders |
| Память > 150MB | Проверь утечки, очисти кеши |

## 🎓 Подробнее

- **README-BENCHMARK.md** - Полная документация
- **BENCHMARK_GUIDE.md** - Детальное руководство по метрикам
- **scripts/analyze-benchmark.js** - Анализатор результатов

---

*Совет: Запускай бенчмарк после каждого значительного изменения кода!*

